"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RemoteJoiner = void 0;
const utils_1 = require("@medusajs/utils");
const catalog_1 = require("./catalog");
const compile_1 = require("./compile");
const execute_1 = require("./execute");
/**
 * Executes cross-module graph queries by resolving relationships from module
 * joiner configs.
 *
 * Pipeline: {@link GraphCatalog} (index configs) → {@link compileQuery}
 * (query → plan) → {@link executePlan} (fetch + join + shortcuts).
 *
 * Data loading is delegated to {@link IRemoteDataFetcher} (typically
 * ModuleDataFetcher). Query is the public entry point that normalizes user
 * input and invokes {@link RemoteJoiner.query}.
 */
class RemoteJoiner {
    constructor(serviceConfigs, dataFetcher, options = {}) {
        this.dataFetcher = dataFetcher;
        this.catalog = new catalog_1.GraphCatalog(serviceConfigs, options);
    }
    async query(queryObj, options) {
        const serviceConfig = this.catalog.getServiceConfig({
            serviceName: queryObj.service,
            serviceAlias: queryObj.alias,
        });
        if (!serviceConfig) {
            if (queryObj.alias) {
                throw new Error(`Service with alias "${queryObj.alias}" was not found.`);
            }
            throw new Error(`Service "${queryObj.service}" was not found.`);
        }
        const initialData = options?.initialData
            ? Array.isArray(options.initialData)
                ? options.initialData
                : [options.initialData]
            : [];
        const plan = (0, compile_1.compileQuery)({
            query: queryObj,
            serviceConfig,
            options,
            initialData,
        }, this.catalog);
        if (options?.throwIfKeyNotFound) {
            if (plan.primaryKeyArg?.value == undefined) {
                if (!plan.primaryKeyArg) {
                    throw new utils_1.MedusaError(utils_1.MedusaError.Types.NOT_FOUND, `${serviceConfig.entity ?? serviceConfig.serviceName}: Primary key(s) [${serviceConfig.primaryKeys.join(", ")}] not found in filters`);
                }
                throw new utils_1.MedusaError(utils_1.MedusaError.Types.NOT_FOUND, `${serviceConfig.entity ?? serviceConfig.serviceName}: Value for primary key ${plan.primaryKeyArg.name} not found in filters`);
            }
        }
        const { data, responsePath, rawResponse, wasArray } = await (0, execute_1.executePlan)({
            plan,
            dataFetcher: this.dataFetcher,
            catalog: this.catalog,
        });
        const retData = wasArray ? data : data[0];
        if (responsePath) {
            rawResponse.data[responsePath] = retData;
        }
        else {
            rawResponse.data = retData;
        }
        return rawResponse.data;
    }
}
exports.RemoteJoiner = RemoteJoiner;
//# sourceMappingURL=remote-joiner.js.map