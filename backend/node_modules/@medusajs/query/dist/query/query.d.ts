import { GraphResultSet, IIndexService, ISearchModuleService, LoadedModule, MedusaContainer, ModuleJoinerConfig, RemoteJoinerOptions, RemoteJoinerQuery, RemoteQueryFilters, RemoteQueryFunction, RemoteQueryInput, RemoteQueryObjectConfig, RemoteQueryObjectFromStringResult, SearchResultSet, SearchTypes } from "@medusajs/types";
import { RelationMap, RemoteJoiner } from "../joiner";
import { ModuleDataFetcher } from "./module-data-fetcher";
/**
 * Public query API for Medusa's cross-module graph query system.
 *
 * Accepts several input shapes (graph config, legacy string config, GraphQL,
 * or a pre-built {@link RemoteJoinerQuery}), normalizes them via
 * {@link normalizeQuery}, and delegates execution to {@link RemoteJoiner}.
 *
 * {@link RemoteJoiner} resolves relationships from module joiner configs,
 * plans nested expands, and loads data through {@link ModuleDataFetcher}
 * ({@link IRemoteDataFetcher}). This class adds response shaping, caching,
 * locale translation, and index-assisted querying on top of that pipeline.
 *
 * ```
 * user input → normalizeQuery() → RemoteJoiner.query() → ModuleDataFetcher.fetch()
 * ```
 */
export declare class Query {
    #private;
    protected container: MedusaContainer;
    static traceGraphQuery?: (queryFn: () => Promise<any>, queryOptions: RemoteQueryInput<any>) => Promise<any>;
    static traceRemoteQuery?: (queryFn: () => Promise<any>, queryOptions: RemoteQueryObjectConfig<any> | RemoteQueryObjectFromStringResult<any> | RemoteJoinerQuery) => Promise<any>;
    static instrument: {
        graphQuery(tracer: (typeof Query)["traceGraphQuery"]): void;
        remoteQuery(tracer: (typeof Query)["traceRemoteQuery"]): void;
        remoteDataFetch(tracer: (typeof ModuleDataFetcher)["traceFetchRemoteData"]): void;
    };
    constructor({ remoteJoiner, joinerConfigs, indexModule, searchModule, container, }: {
        remoteJoiner: RemoteJoiner;
        joinerConfigs: ModuleJoinerConfig[];
        indexModule: IIndexService;
        searchModule?: ISearchModuleService;
        container: MedusaContainer;
    });
    query(queryOptions: RemoteQueryInput<any> | RemoteQueryObjectConfig<any> | RemoteQueryObjectFromStringResult<any> | RemoteJoinerQuery, options?: RemoteJoinerOptions): Promise<any>;
    gql(query: string, variables?: Record<string, unknown>, options?: RemoteJoinerOptions): Promise<any>;
    graph<const TEntry extends string>(queryOptions: RemoteQueryInput<TEntry>, options?: RemoteJoinerOptions): Promise<GraphResultSet<TEntry>>;
    /**
     * Runs a search and expands the hits with `query.graph`.
     *
     * The engine only holds what its index was defined with, so the requested
     * fields are split: what the index can return comes back from the search, and
     * the rest is fetched by id. Search order is preserved through the expand.
     */
    search<const TEntry extends string>(queryOptions: SearchTypes.SearchQuery<TEntry>, options?: RemoteJoinerOptions): Promise<SearchResultSet<TEntry>>;
    index<const TEntry extends string>(queryOptions: RemoteQueryInput<TEntry> & {
        joinFilters?: RemoteQueryFilters<TEntry>;
    }, options?: RemoteJoinerOptions): Promise<GraphResultSet<TEntry>>;
    private internalQuery;
}
export declare function createQuery({ modulesLoaded, relationMap, indexModule, searchModule, container, }: {
    modulesLoaded: LoadedModule[];
    relationMap: RelationMap;
    indexModule: IIndexService;
    searchModule?: ISearchModuleService;
    container: MedusaContainer;
}): Omit<RemoteQueryFunction, symbol>;
export declare function parseGraphqlQuery(graphqlQuery: string, variables?: Record<string, unknown>): RemoteJoinerQuery;
//# sourceMappingURL=query.d.ts.map