import { WorkflowTypes } from "@medusajs/framework/types";
/**
 * The step ID for exporting inventory items.
 */
export declare const exportInventoryItemsStepId = "export-inventory-items";
/**
 * This step exports inventory items to a CSV file based on the provided filters.
 * The exported file includes the inventory levels of each item per stock location.
 *
 * @example
 * To export all inventory items:
 *
 * ```ts
 * const data = exportInventoryItemsStep({
 *   select: ["id", "sku", "title"],
 *   batch_size: 100
 * })
 * ```
 *
 * To export inventory items stocked in a specific location:
 *
 * ```ts
 * const data = exportInventoryItemsStep({
 *   select: ["id", "sku", "title"],
 *   filter: {
 *     location_levels: {
 *       location_id: "sloc_123"
 *     }
 *   }
 * })
 * ```
 */
export declare const exportInventoryItemsStep: import("@medusajs/framework/workflows-sdk").StepFunction<WorkflowTypes.InventoryWorkflow.ExportInventoryItemsDTO, {
    id: string;
    filename: string;
}>;
//# sourceMappingURL=export-inventory-items.d.ts.map