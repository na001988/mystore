"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeCustomerAccountWorkflow = exports.removeCustomerAccountWorkflowId = exports.getCustomerAuthIdentityStep = void 0;
const utils_1 = require("@medusajs/framework/utils");
const workflows_sdk_1 = require("@medusajs/framework/workflows-sdk");
const auth_1 = require("../../auth");
const common_1 = require("../../common");
const delete_customers_1 = require("./delete-customers");
/**
 * This step returns the customer's auth identity from a queried list. It throws
 * an error if no auth identity is found.
 */
exports.getCustomerAuthIdentityStep = (0, workflows_sdk_1.createStep)("get-customer-auth-identity", async ({ authIdentities }) => {
    const authIdentity = authIdentities[0];
    if (!authIdentity) {
        throw new utils_1.MedusaError(utils_1.MedusaError.Types.NOT_FOUND, "Auth identity not found");
    }
    return new workflows_sdk_1.StepResponse(authIdentity);
});
exports.removeCustomerAccountWorkflowId = "remove-customer-account";
/**
 * This workflow deletes a customer and remove its association to its auth identity. It's used by the
 * [Delete Customer Admin API Route](https://docs.medusajs.com/api/admin/customers/delete-a-customer).
 *
 * You can use this workflow within your customizations or your own custom workflows, allowing you to
 * delete customer accounts within your custom flows.
 *
 * :::note
 *
 * This workflow uses the {@link deleteCustomersWorkflow} which has a hook that allows you to perform
 * custom actions after the customers are deleted.
 *
 * :::
 *
 * @example
 * const { result } = await removeCustomerAccountWorkflow(container)
 * .run({
 *   input: {
 *     customerId: "cus_123"
 *   }
 * })
 *
 * @summary
 *
 * Delete a customer account and its auth identity association.
 */
exports.removeCustomerAccountWorkflow = (0, workflows_sdk_1.createWorkflow)(exports.removeCustomerAccountWorkflowId, (input) => {
    const { data: customer } = (0, common_1.useQueryGraphStep)({
        entity: "customer",
        fields: ["id", "has_account"],
        filters: {
            id: input.customerId,
        },
        options: {
            isList: false,
        },
    }).config({ name: "get-customer" });
    delete_customers_1.deleteCustomersWorkflow.runAsStep({
        input: {
            ids: [input.customerId],
        },
    });
    const authIdentity = (0, workflows_sdk_1.when)({ customer }, ({ customer }) => {
        return !!customer?.has_account;
    }).then(() => {
        const { data: authIdentities } = (0, common_1.useQueryGraphStep)({
            entity: "auth_identity",
            fields: ["id", "app_metadata", "provider_identities.entity_id"],
            filters: {
                app_metadata: {
                    customer_id: input.customerId,
                },
            },
        }).config({ name: "get-auth-identities" });
        return (0, exports.getCustomerAuthIdentityStep)({ authIdentities });
    });
    const shouldKeepAuthIdentity = (0, workflows_sdk_1.transform)({ authIdentity }, ({ authIdentity }) => {
        if (!authIdentity) {
            return undefined;
        }
        // Only keep the auth identity if it has other actor types associated with it
        return Object.entries(authIdentity.app_metadata ?? {})
            .filter(([key, _]) => key !== "customer_id")
            .some(([_, value]) => value !== null);
    });
    (0, workflows_sdk_1.when)({ shouldKeepAuthIdentity }, ({ shouldKeepAuthIdentity }) => {
        return shouldKeepAuthIdentity === true;
    }).then(() => {
        // we don't remove a matching entity_id provider_entity, since it could be used by the remaining
        // actor types.
        (0, auth_1.setAuthAppMetadataStep)({
            authIdentityId: authIdentity.id,
            actorType: "customer",
            value: null,
        });
    });
    (0, workflows_sdk_1.when)({ shouldKeepAuthIdentity }, ({ shouldKeepAuthIdentity }) => {
        return shouldKeepAuthIdentity === false;
    }).then(() => {
        (0, auth_1.deleteAuthIdentityStep)({
            id: authIdentity.id,
        });
    });
    return new workflows_sdk_1.WorkflowResponse(input.customerId);
});
//# sourceMappingURL=remove-customer-account.js.map