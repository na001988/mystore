"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deleteAuthIdentityStep = exports.deleteAuthIdentityStepId = void 0;
const workflows_sdk_1 = require("@medusajs/framework/workflows-sdk");
const utils_1 = require("@medusajs/framework/utils");
exports.deleteAuthIdentityStepId = "delete-auth-identity";
/**
 * This step deletes one or more auth identities.
 *
 * @since 2.18.1
 *
 * @example
 * To delete a single auth identity:
 * ```ts
 * deleteAuthIdentityStep({
 *   id: "authid_1234",
 * })
 * ```
 *
 * To delete multiple auth identities:
 * ```ts
 * deleteAuthIdentityStep({
 *   id: ["authid_1234", "authid_1235"],
 * })
 * ```
 */
exports.deleteAuthIdentityStep = (0, workflows_sdk_1.createStep)(exports.deleteAuthIdentityStepId, async (input, { container }) => {
    const authModuleService = container.resolve(utils_1.Modules.AUTH);
    const normalizedInput = Array.isArray(input.id) ? input.id : [input.id];
    const prev = await authModuleService.listAuthIdentities({ id: normalizedInput }, { relations: ["provider_identities"] });
    await authModuleService.deleteAuthIdentities(normalizedInput);
    return new workflows_sdk_1.StepResponse(void 0, prev);
}, async (prev, { container }) => {
    const authModuleService = container.resolve(utils_1.Modules.AUTH);
    if (!prev) {
        return;
    }
    await authModuleService.createAuthIdentities(prev);
});
//# sourceMappingURL=delete-auth-identity.js.map