"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.setActiveLayoutScopeStep = exports.setActiveLayoutScopeStepId = void 0;
const utils_1 = require("@medusajs/framework/utils");
const workflows_sdk_1 = require("@medusajs/framework/workflows-sdk");
exports.setActiveLayoutScopeStepId = "set-active-layout-scope";
/**
 * This step sets a user's active layout scope for a zone, which determines
 * whether the user's personal or the system default configuration is shown.
 *
 * @since 2.17.2
 *
 * @example
 * const data = setActiveLayoutScopeStep({
 *   zone: "products",
 *   user_id: "user_123",
 *   scope: "personal",
 * })
 */
exports.setActiveLayoutScopeStep = (0, workflows_sdk_1.createStep)(exports.setActiveLayoutScopeStepId, async (input, { container }) => {
    const service = container.resolve(utils_1.Modules.SETTINGS);
    const previousScope = await service.getActiveLayoutScope(input.zone, input.user_id);
    await service.setActiveLayoutScope(input.zone, input.user_id, input.scope ?? null);
    return new workflows_sdk_1.StepResponse(input.scope, {
        zone: input.zone,
        user_id: input.user_id,
        previousScope,
    });
}, async (compensateInput, { container }) => {
    if (!compensateInput) {
        return;
    }
    const service = container.resolve(utils_1.Modules.SETTINGS);
    await service.setActiveLayoutScope(compensateInput.zone, compensateInput.user_id, compensateInput.previousScope);
});
//# sourceMappingURL=set-active-layout-scope.js.map