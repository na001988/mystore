"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.clearLayoutConfigurationStep = exports.clearLayoutConfigurationStepId = void 0;
const utils_1 = require("@medusajs/framework/utils");
const workflows_sdk_1 = require("@medusajs/framework/workflows-sdk");
exports.clearLayoutConfigurationStepId = "clear-layout-configuration";
/**
 * This step clears a user's personal layout configuration for a zone,
 * reverting them to the system default.
 *
 * @since 2.17.2
 *
 * @example
 * const data = clearLayoutConfigurationStep({
 *   zone: "products",
 *   user_id: "user_123",
 * })
 */
exports.clearLayoutConfigurationStep = (0, workflows_sdk_1.createStep)(exports.clearLayoutConfigurationStepId, async (input, { container }) => {
    const service = container.resolve(utils_1.Modules.SETTINGS);
    const [previous] = await service.listLayoutConfigurations({ zone: input.zone, user_id: input.user_id }, { take: 1 });
    await service.clearLayoutConfiguration(input.zone, input.user_id);
    return new workflows_sdk_1.StepResponse(void 0, {
        zone: input.zone,
        user_id: input.user_id,
        previousConfiguration: previous?.configuration ?? null,
    });
}, async (compensateInput, { container }) => {
    if (!compensateInput?.previousConfiguration) {
        return;
    }
    const service = container.resolve(utils_1.Modules.SETTINGS);
    await service.setLayoutConfiguration(compensateInput.zone, compensateInput.user_id, compensateInput.previousConfiguration);
});
//# sourceMappingURL=clear-layout-configuration.js.map