/**
 * @ignore
 * @featureFlag rbac
 * @since 2.16.0
 */
export type GetAssignableRolesStepInput = {
    /**
     * Actor ID whose assignability is evaluated.
     */
    actor_id: string;
    /**
     * Actor entity name. Defaults to "user".
     */
    actor?: string;
    /**
     * Optional filters forwarded to the `rbac_role` query (e.g. `q`, `id`).
     */
    filters?: Record<string, unknown>;
    /**
     * Optional pagination forwarded to the `rbac_role` query.
     */
    pagination?: {
        skip?: number;
        take?: number;
    };
};
type AssignableRole = {
    id: string;
    name: string;
    description: string | null;
};
/**
 * @ignore
 * @featureFlag rbac
 * @since 2.16.0
 */
export type GetAssignableRolesStepOutput = {
    roles: AssignableRole[];
    count: number;
};
/**
 * @ignore
 * @featureFlag rbac
 * @since 2.16.0
 */
export declare const getAssignableRolesStepId = "get-assignable-roles";
/**
 * Resolves the set of roles the actor is allowed to assign.
 *
 * @ignore
 * @featureFlag rbac
 * @since 2.16.0
 */
export declare const getAssignableRolesStep: import("@medusajs/framework/workflows-sdk").StepFunction<GetAssignableRolesStepInput, GetAssignableRolesStepOutput>;
export {};
//# sourceMappingURL=get-assignable-roles.d.ts.map