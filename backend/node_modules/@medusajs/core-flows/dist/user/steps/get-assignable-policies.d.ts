/**
 * @ignore
 * @featureFlag rbac
 * @since 2.16.0
 */
export type GetAssignablePoliciesStepInput = {
    /**
     * Actor ID whose assignability is evaluated.
     */
    actor_id: string;
    /**
     * Actor entity name. Defaults to "user".
     */
    actor?: string;
    /**
     * Optional filters forwarded to the `rbac_policy` query (e.g. `q`, `id`, `resource`, `operation`).
     */
    filters?: Record<string, unknown>;
    /**
     * Optional pagination applied to the assignable subset after permission resolution.
     */
    pagination?: {
        skip?: number;
        take?: number;
    };
};
type AssignablePolicy = {
    id: string;
    key: string;
    resource: string;
    operation: string;
    description: string | null;
};
/**
 * @ignore
 * @featureFlag rbac
 * @since 2.16.0
 */
export type GetAssignablePoliciesStepOutput = {
    policies: AssignablePolicy[];
    count: number;
};
/**
 * @ignore
 * @featureFlag rbac
 * @since 2.16.0
 */
export declare const getAssignablePoliciesStepId = "get-assignable-policies";
/**
 * Resolves the set of policies the actor is allowed to assign.
 *
 * @ignore
 * @featureFlag rbac
 * @since 2.16.0
 */
export declare const getAssignablePoliciesStep: import("@medusajs/framework/workflows-sdk").StepFunction<GetAssignablePoliciesStepInput, GetAssignablePoliciesStepOutput>;
export {};
//# sourceMappingURL=get-assignable-policies.d.ts.map