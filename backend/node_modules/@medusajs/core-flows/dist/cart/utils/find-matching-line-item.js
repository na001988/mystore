"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.lineItemFieldsForMerging = void 0;
exports.findMatchingLineItem = findMatchingLineItem;
const utils_1 = require("@medusajs/framework/utils");
const metadataMatches = (existingItem, newItem) => (!(0, utils_1.isPresent)(existingItem?.metadata) && !(0, utils_1.isPresent)(newItem.metadata)) ||
    (0, utils_1.deepEqualObj)(existingItem?.metadata, newItem.metadata);
/**
 * The line item fields that must be selected when looking for existing cart
 * line items a new item can be merged into.
 */
exports.lineItemFieldsForMerging = [
    "id",
    "metadata",
    "variant_id",
    "quantity",
    "unit_price",
    "compare_at_unit_price",
    "is_custom_price",
];
/**
 * Finds the line item, among the existing items of the same variant, that a new
 * item should be merged into. Items are merged when their metadata matches and
 * either both carry the same custom price, or neither has a custom price.
 */
function findMatchingLineItem(existingItems, newItem) {
    return existingItems?.find((existingItem) => newItem.is_custom_price
        ? metadataMatches(existingItem, newItem) &&
            newItem.unit_price === existingItem.unit_price
        : metadataMatches(existingItem, newItem) && !existingItem.is_custom_price);
}
//# sourceMappingURL=find-matching-line-item.js.map