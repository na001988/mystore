"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getLineItemPricingQuantitiesStep = exports.getLineItemPricingQuantitiesStepId = void 0;
const utils_1 = require("@medusajs/framework/utils");
const workflows_sdk_1 = require("@medusajs/framework/workflows-sdk");
const find_matching_line_item_1 = require("../utils/find-matching-line-item");
exports.getLineItemPricingQuantitiesStepId = "get-line-item-pricing-quantities";
/**
 * This step resolves the quantity that each item added to a cart must be priced
 * for. When an item is merged into an existing line item of the cart, its price
 * has to be selected for the resulting total quantity, not only for the added
 * quantity. Otherwise, quantity-based prices (such as price list tiers) are
 * resolved against the wrong quantity.
 *
 * The returned quantities are aligned, by index, with the input items.
 *
 * @example
 * const quantities = getLineItemPricingQuantitiesStep({
 *   "id": "cart_123",
 *   "items": [{
 *     "variant_id": "variant_123",
 *     "quantity": 1,
 *   }]
 * })
 */
exports.getLineItemPricingQuantitiesStep = (0, workflows_sdk_1.createStep)(exports.getLineItemPricingQuantitiesStepId, async (data, { container }) => {
    const items = data.items ?? [];
    const quantities = items.map((item) => utils_1.MathBN.convert(item.quantity ?? 1).toNumber());
    const variantIds = items
        .map((item) => item.variant_id)
        .filter((id) => !!id);
    if (!variantIds.length) {
        return new workflows_sdk_1.StepResponse(quantities);
    }
    const cartModule = container.resolve(utils_1.Modules.CART);
    const existingVariantItems = await cartModule.listLineItems({
        cart_id: data.id,
        variant_id: variantIds,
    }, {
        select: find_matching_line_item_1.lineItemFieldsForMerging,
    });
    const variantItemsMap = existingVariantItems.reduce((result, variantItem) => {
        if (!result.has(variantItem.variant_id)) {
            result.set(variantItem.variant_id, []);
        }
        result.get(variantItem.variant_id).push(variantItem);
        return result;
    }, new Map());
    items.forEach((item, index) => {
        if (!item.variant_id) {
            return;
        }
        const existingItem = (0, find_matching_line_item_1.findMatchingLineItem)(variantItemsMap.get(item.variant_id), {
            metadata: item.metadata,
            unit_price: item.unit_price,
            is_custom_price: (0, utils_1.isDefined)(item.unit_price),
        });
        if (!existingItem) {
            return;
        }
        const quantity = utils_1.MathBN.sum(existingItem.quantity, item.quantity ?? 1);
        // In case of multiple items merging into the same line item, the
        // quantities accumulate, the same way they do when the items are updated.
        existingItem.quantity = quantity;
        quantities[index] = quantity.toNumber();
    });
    return new workflows_sdk_1.StepResponse(quantities);
});
//# sourceMappingURL=get-line-item-pricing-quantities.js.map