"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.authorizePaymentSessionForOrderWorkflow = exports.authorizePaymentSessionForOrderWorkflowId = void 0;
const utils_1 = require("@medusajs/framework/utils");
const workflows_sdk_1 = require("@medusajs/framework/workflows-sdk");
const common_1 = require("../../common");
const add_order_transaction_1 = require("../../order/steps/add-order-transaction");
const steps_1 = require("../steps");
const validatePendingAuthorizationStepId = "validate-pending-authorization-step";
/**
 * This step validates that the payment session is in pending_authorization status.
 */
const validatePendingAuthorizationStep = (0, workflows_sdk_1.createStep)(validatePendingAuthorizationStepId, async (input) => {
    if (input.status !== utils_1.PaymentSessionStatus.PENDING_AUTHORIZATION) {
        throw new utils_1.MedusaError(utils_1.MedusaError.Types.NOT_ALLOWED, `Payment session ${input.payment_session_id} is not in pending_authorization status`);
    }
    return new workflows_sdk_1.StepResponse(void 0);
});
exports.authorizePaymentSessionForOrderWorkflowId = "authorize-payment-session-for-order";
/**
 * This workflow authorizes a payment session that is in pending_authorization status,
 * typically triggered by an admin action when a deferred payment (bank transfer,
 * payment link) needs to be manually checked or authorized.
 *
 * If the provider confirms authorization, a Payment record is created and order
 * transactions are added for any captures. If the provider still returns
 * pending_authorization, no changes are made.
 *
 * @summary
 *
 * Authorize a pending payment session for an existing order.
 */
exports.authorizePaymentSessionForOrderWorkflow = (0, workflows_sdk_1.createWorkflow)(exports.authorizePaymentSessionForOrderWorkflowId, (input) => {
    const { data: paymentSession } = (0, common_1.useQueryGraphStep)({
        entity: "payment_session",
        fields: ["id", "status", "payment_collection_id"],
        filters: { id: input.payment_session_id },
        options: { throwIfKeyNotFound: true, isList: false },
    }).config({ name: "get-payment-session" });
    validatePendingAuthorizationStep({
        status: paymentSession.status,
        payment_session_id: input.payment_session_id,
    });
    const payment = (0, steps_1.authorizePaymentSessionStep)({
        id: input.payment_session_id,
    });
    const { data: orderPaymentCollection } = (0, common_1.useQueryGraphStep)({
        entity: "order_payment_collection",
        fields: ["order.id"],
        filters: {
            payment_collection_id: paymentSession.payment_collection_id,
        },
        options: { isList: false },
    }).config({ name: "get-order-payment-collection" });
    (0, workflows_sdk_1.when)("add-order-transactions", { payment, orderPaymentCollection }, ({ payment, orderPaymentCollection }) => {
        return !!payment && !!orderPaymentCollection?.order?.id;
    }).then(() => {
        const orderTransactions = (0, workflows_sdk_1.transform)({ payment, orderPaymentCollection }, ({ payment, orderPaymentCollection }) => {
            return (payment?.captures?.map((capture) => {
                return {
                    order_id: orderPaymentCollection.order.id,
                    amount: capture.amount,
                    currency_code: payment.currency_code,
                    reference_id: capture.id,
                    reference: "capture",
                };
            }) ?? []);
        });
        (0, add_order_transaction_1.addOrderTransactionStep)(orderTransactions);
    });
    return new workflows_sdk_1.WorkflowResponse(payment);
});
//# sourceMappingURL=authorize-payment-session-for-order.js.map