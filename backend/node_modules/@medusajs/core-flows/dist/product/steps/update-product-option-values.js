"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateProductOptionValuesStep = exports.updateProductOptionValuesStepId = void 0;
const utils_1 = require("@medusajs/framework/utils");
const workflows_sdk_1 = require("@medusajs/framework/workflows-sdk");
exports.updateProductOptionValuesStepId = "update-product-option-values";
/**
 * This step updates a product option value.
 *
 * @example
 * const data = updateProductOptionValuesStep({
 *   id: "optval_123",
 *   update: {
 *     metadata: { is_default: true }
 *   }
 * })
 */
exports.updateProductOptionValuesStep = (0, workflows_sdk_1.createStep)(exports.updateProductOptionValuesStepId, async (data, { container }) => {
    const service = container.resolve(utils_1.Modules.PRODUCT);
    const prevData = await service.retrieveProductOptionValue(data.id, {
        select: ["id", "value", "metadata"],
    });
    const productOptionValue = await service.updateProductOptionValues(data.id, data.update);
    return new workflows_sdk_1.StepResponse(productOptionValue, prevData);
}, async (prevData, { container }) => {
    if (!prevData) {
        return;
    }
    const service = container.resolve(utils_1.Modules.PRODUCT);
    await service.updateProductOptionValues(prevData.id, {
        value: prevData.value,
        metadata: prevData.metadata,
    });
});
//# sourceMappingURL=update-product-option-values.js.map