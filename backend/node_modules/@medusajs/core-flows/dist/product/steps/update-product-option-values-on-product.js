"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateProductOptionValuesOnProductStep = exports.updateProductOptionValuesOnProductStepId = void 0;
const utils_1 = require("@medusajs/framework/utils");
const workflows_sdk_1 = require("@medusajs/framework/workflows-sdk");
exports.updateProductOptionValuesOnProductStepId = "update-product-option-values-on-product";
/**
 * This step updates product option values linked to products.
 */
exports.updateProductOptionValuesOnProductStep = (0, workflows_sdk_1.createStep)(exports.updateProductOptionValuesOnProductStepId, async (updates, { container }) => {
    const effectiveUpdates = updates.filter((pair) => pair.add?.length || pair.remove?.length);
    if (!effectiveUpdates.length) {
        return new workflows_sdk_1.StepResponse(void 0, []);
    }
    const service = container.resolve(utils_1.Modules.PRODUCT);
    const productIds = [
        ...new Set(effectiveUpdates.map((pair) => pair.product_id)),
    ];
    const products = await service.listProducts({ id: productIds }, { relations: ["options.values"] });
    const existingValueIdsByPairKey = new Map();
    for (const product of products) {
        for (const option of product.options ?? []) {
            const valueIds = (option.values ?? []).map((value) => value.id);
            existingValueIdsByPairKey.set(`${product.id}_${option.id}`, new Set(valueIds));
        }
    }
    await service.updateProductOptionValuesOnProduct(effectiveUpdates);
    const updatedProducts = await service.listProducts({ id: productIds }, { relations: ["options.values"] });
    const updatedValueIdsByPairKey = new Map();
    for (const product of updatedProducts) {
        for (const option of product.options ?? []) {
            const valueIds = (option.values ?? []).map((value) => value.id);
            updatedValueIdsByPairKey.set(`${product.id}_${option.id}`, new Set(valueIds));
        }
    }
    const compensation = effectiveUpdates
        .map((pair) => {
        const key = `${pair.product_id}_${pair.product_option_id}`;
        const existingValueIds = existingValueIdsByPairKey.get(key) ?? new Set();
        const updatedValueIds = updatedValueIdsByPairKey.get(key) ?? new Set();
        const actualAdds = [...updatedValueIds].filter((valueId) => !existingValueIds.has(valueId));
        const actualRemoves = [...existingValueIds].filter((valueId) => !updatedValueIds.has(valueId));
        return {
            product_id: pair.product_id,
            product_option_id: pair.product_option_id,
            add: actualRemoves,
            remove: actualAdds,
        };
    })
        .filter((pair) => pair.add?.length || pair.remove?.length);
    return new workflows_sdk_1.StepResponse(void 0, compensation);
}, async (compensation, { container }) => {
    if (!compensation?.length) {
        return;
    }
    const service = container.resolve(utils_1.Modules.PRODUCT);
    await service.updateProductOptionValuesOnProduct(compensation);
});
//# sourceMappingURL=update-product-option-values-on-product.js.map