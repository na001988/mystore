"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.inferLinkType = inferLinkType;
exports.qualifyTable = qualifyTable;
exports.getTargetPrimaryKey = getTargetPrimaryKey;
exports.buildLinkCorrelationSql = buildLinkCorrelationSql;
exports.buildTargetCorrelationSql = buildTargetCorrelationSql;
exports.buildLinkToTargetJoinSql = buildLinkToTargetJoinSql;
exports.buildLinkSoftDeleteSql = buildLinkSoftDeleteSql;
exports.buildTargetSoftDeleteSql = buildTargetSoftDeleteSql;
exports.quoteIdentifier = quoteIdentifier;
exports.resolveCrossModuleJoins = resolveCrossModuleJoins;
const common_1 = require("../../../common");
function inferLinkType(joinSpec, correlateSpec) {
    if (joinSpec.link.schema === joinSpec.target.schema &&
        joinSpec.link.table === joinSpec.target.table &&
        joinSpec.link.targetKey === (joinSpec.target.primaryKey ?? "id")) {
        return "target";
    }
    if (joinSpec.link.table === correlateSpec.table &&
        joinSpec.link.sourceKey === correlateSpec.key) {
        return "source";
    }
    return "pivot";
}
function qualifyTable(schema, table, defaultSchema) {
    const resolvedSchema = schema ?? defaultSchema;
    return `${quoteIdentifier(resolvedSchema)}.${quoteIdentifier(table)}`;
}
function getTargetPrimaryKey(joinSpec) {
    return joinSpec.target.primaryKey ?? "id";
}
function buildLinkCorrelationSql(joinSpec, correlateSpec, linkAlias) {
    return `${quoteIdentifier(linkAlias)}.${quoteIdentifier(joinSpec.link.sourceKey)} = ${quoteIdentifier(correlateSpec.alias)}.${quoteIdentifier(correlateSpec.key)}`;
}
function buildTargetCorrelationSql(joinSpec, correlateSpec, targetAlias, linkType) {
    if (linkType === "target") {
        return `${quoteIdentifier(targetAlias)}.${quoteIdentifier(joinSpec.link.sourceKey)} = ${quoteIdentifier(correlateSpec.alias)}.${quoteIdentifier(correlateSpec.key)}`;
    }
    if (linkType === "source") {
        return `${quoteIdentifier(targetAlias)}.${quoteIdentifier(getTargetPrimaryKey(joinSpec))} = ${quoteIdentifier(correlateSpec.alias)}.${quoteIdentifier(joinSpec.link.targetKey)}`;
    }
    return "";
}
function buildLinkToTargetJoinSql(joinSpec, linkAlias, targetAlias) {
    return `${quoteIdentifier(linkAlias)}.${quoteIdentifier(joinSpec.link.targetKey)} = ${quoteIdentifier(targetAlias)}.${quoteIdentifier(getTargetPrimaryKey(joinSpec))}`;
}
function buildLinkSoftDeleteSql(linkAlias, withDeleted) {
    if (withDeleted) {
        return "";
    }
    return `${quoteIdentifier(linkAlias)}."deleted_at" is null`;
}
function buildTargetSoftDeleteSql(targetAlias, withDeleted) {
    if (withDeleted) {
        return "";
    }
    return `${quoteIdentifier(targetAlias)}."deleted_at" is null`;
}
function quoteIdentifier(identifier) {
    return `"${identifier.replace(/"/g, '""')}"`;
}
function resolveCrossModuleJoins(crossModuleJoins) {
    assertValidCrossModuleJoins(crossModuleJoins);
    const usedBaseAliases = new Map();
    return crossModuleJoins.map((join) => {
        const baseAlias = join.target.table;
        const usageCount = usedBaseAliases.get(baseAlias) ?? 0;
        usedBaseAliases.set(baseAlias, usageCount + 1);
        const alias = usageCount === 0 ? baseAlias : `${baseAlias}_${usageCount + 1}`;
        return {
            ...join,
            alias,
        };
    });
}
function assertValidCrossModuleJoins(crossModuleJoins) {
    const targetTables = new Set();
    for (const join of crossModuleJoins) {
        if (targetTables.has(join.target.table)) {
            throw new common_1.MedusaError(common_1.MedusaError.Types.INVALID_DATA, `Duplicate cross-module join target table "${join.target.table}". Each join must target a unique table.`);
        }
        targetTables.add(join.target.table);
    }
    for (const join of crossModuleJoins) {
        if (!join.parent) {
            continue;
        }
        if (join.parent === join.target.table) {
            throw new common_1.MedusaError(common_1.MedusaError.Types.INVALID_DATA, `Cross-module join for "${join.target.table}" cannot be its own parent.`);
        }
        if (!targetTables.has(join.parent)) {
            throw new common_1.MedusaError(common_1.MedusaError.Types.INVALID_DATA, `Cross-module join for "${join.target.table}" references unknown parent target table "${join.parent}".`);
        }
    }
}
//# sourceMappingURL=helpers.js.map