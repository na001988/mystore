import { DAL } from "@medusajs/types";
export type AugmentFindOptionsWithCrossModuleJoinsArgs = {
    entityName: string;
    entityTable: string;
    primaryKey?: string;
    defaultSchema?: string;
};
/**
 * Translates cross-module join metadata into MikroORM-compatible `where` and
 * `orderBy` clauses so callers can keep using `manager.find()` / `findAndCount()`.
 */
export declare function augmentFindOptionsWithCrossModuleJoins<const T>(findOptions: DAL.FindOptions<T>, { entityName, entityTable, primaryKey, defaultSchema, }: AugmentFindOptionsWithCrossModuleJoinsArgs): DAL.FindOptions<T>;
//# sourceMappingURL=augment-find-options.d.ts.map