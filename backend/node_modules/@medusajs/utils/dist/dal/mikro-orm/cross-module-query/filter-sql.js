"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildFilterSql = buildFilterSql;
exports.joinRequiresFilter = joinRequiresFilter;
exports.buildExistsFilter = buildExistsFilter;
const core_1 = require("@medusajs/deps/mikro-orm/core");
const common_1 = require("../../../common");
const helpers_1 = require("./helpers");
function buildFilterSql(alias, filters) {
    const clauses = [];
    const bindings = [];
    for (const [key, value] of Object.entries(filters)) {
        if (key === "$and" && Array.isArray(value)) {
            for (const condition of value) {
                const nested = buildFilterSql(alias, condition);
                // Skip conditions that produced no SQL to avoid emitting `()`.
                if (!nested.sql) {
                    continue;
                }
                clauses.push(`(${nested.sql})`);
                bindings.push(...nested.bindings);
            }
            continue;
        }
        if (key === "$or" && Array.isArray(value)) {
            const nestedClauses = [];
            const nestedBindings = [];
            for (const condition of value) {
                const nested = buildFilterSql(alias, condition);
                // Skip conditions that produced no SQL to avoid emitting empty
                // groups like `()`, which are invalid SQL.
                if (!nested.sql) {
                    continue;
                }
                nestedClauses.push(`(${nested.sql})`);
                nestedBindings.push(...nested.bindings);
            }
            // An empty (or fully-undefined) `$or` places no constraint at all.
            if (!nestedClauses.length) {
                continue;
            }
            clauses.push(`(${nestedClauses.join(" or ")})`);
            bindings.push(...nestedBindings);
            continue;
        }
        const column = `${(0, helpers_1.quoteIdentifier)(alias)}.${(0, helpers_1.quoteIdentifier)(key)}`;
        if (value === null) {
            clauses.push(`${column} is null`);
            continue;
        }
        if (value === undefined) {
            continue;
        }
        if (Array.isArray(value)) {
            const inArraySql = buildInArraySql(column, value, false);
            if (inArraySql) {
                clauses.push(inArraySql.sql);
                bindings.push(...inArraySql.bindings);
            }
            continue;
        }
        if ((0, common_1.isObject)(value)) {
            const operatorKeys = Object.keys(value).filter((operator) => operator.startsWith("$"));
            if (operatorKeys.length) {
                for (const [operator, operatorValue] of Object.entries(value)) {
                    const operatorSql = buildOperatorSql(column, operator, operatorValue, bindings);
                    if (operatorSql) {
                        clauses.push(operatorSql);
                    }
                }
                continue;
            }
        }
        clauses.push(`${column} = ?`);
        bindings.push(value);
    }
    return {
        sql: clauses.join(" and "),
        bindings,
    };
}
function joinRequiresFilter(joinSpec, childrenByParent) {
    if (hasTargetFilters(joinSpec)) {
        return true;
    }
    const children = childrenByParent.get(joinSpec.alias) ?? [];
    return children.some((child) => joinRequiresFilter(child, childrenByParent));
}
function buildExistsFilter(joinSpec, correlateSpec, context) {
    const fragment = buildExistsSql(joinSpec, correlateSpec, context);
    return {
        [(0, core_1.raw)(fragment.sql, fragment.bindings)]: [],
    };
}
function buildExistsSql(joinSpec, correlateSpec, context) {
    const linkType = (0, helpers_1.inferLinkType)(joinSpec, correlateSpec);
    const targetPrimaryKey = (0, helpers_1.getTargetPrimaryKey)(joinSpec);
    // An optimization for filters that only apply on the target's primary key, useful to support these filters on non-DB external modules
    const onlyTargetIdFilters = !!joinSpec.target.filters &&
        Object.keys(joinSpec.target.filters).length === 1 &&
        joinSpec.target.filters[targetPrimaryKey] !== undefined;
    if (onlyTargetIdFilters && linkType === "pivot") {
        return buildTargetIdOnlyExistsSql(joinSpec, correlateSpec, context);
    }
    if (linkType === "pivot") {
        return buildPivotLinkExistsSql(joinSpec, correlateSpec, context);
    }
    else {
        return buildNoLinkExistsSql(joinSpec, correlateSpec, context, linkType);
    }
}
function buildTargetIdOnlyExistsSql(joinSpec, correlateSpec, context) {
    const targetPrimaryKey = (0, helpers_1.getTargetPrimaryKey)(joinSpec);
    const linkAlias = nextLinkAlias(context);
    const linkTable = (0, helpers_1.qualifyTable)(joinSpec.link.schema, joinSpec.link.table, context.defaultSchema);
    const bindings = [];
    const clauses = [
        (0, helpers_1.buildLinkCorrelationSql)(joinSpec, correlateSpec, linkAlias),
        (0, helpers_1.buildLinkSoftDeleteSql)(linkAlias, context.withDeleted),
    ];
    const linkFilters = buildFilterSql(linkAlias, {
        [joinSpec.link.targetKey]: joinSpec.target.filters?.[targetPrimaryKey],
    });
    clauses.push(linkFilters.sql);
    bindings.push(...linkFilters.bindings);
    const sql = `exists (select 1 from ${linkTable} as ${(0, helpers_1.quoteIdentifier)(linkAlias)} where ${clauses.filter(Boolean).join(" and ")})`;
    return { sql, bindings };
}
function buildPivotLinkExistsSql(joinSpec, correlateSpec, context) {
    const linkAlias = nextLinkAlias(context);
    const linkTable = (0, helpers_1.qualifyTable)(joinSpec.link.schema, joinSpec.link.table, context.defaultSchema);
    const targetAlias = joinSpec.alias;
    const targetTable = (0, helpers_1.qualifyTable)(joinSpec.target.schema, joinSpec.target.table, context.defaultSchema);
    const bindings = [];
    const clauses = [
        (0, helpers_1.buildLinkCorrelationSql)(joinSpec, correlateSpec, linkAlias),
        (0, helpers_1.buildLinkToTargetJoinSql)(joinSpec, linkAlias, targetAlias),
        (0, helpers_1.buildLinkSoftDeleteSql)(linkAlias, context.withDeleted),
        (0, helpers_1.buildTargetSoftDeleteSql)(targetAlias, context.withDeleted),
    ];
    const targetFilters = getTargetFilters(joinSpec, targetAlias, context);
    clauses.push(...targetFilters.clauses);
    bindings.push(...targetFilters.bindings);
    const sql = `exists (select 1 from ${linkTable} as ${(0, helpers_1.quoteIdentifier)(linkAlias)} inner join ${targetTable} as ${(0, helpers_1.quoteIdentifier)(targetAlias)} on true where ${clauses.filter(Boolean).join(" and ")})`;
    return {
        sql,
        bindings,
    };
}
function buildNoLinkExistsSql(joinSpec, correlateSpec, context, linkType) {
    const targetAlias = joinSpec.alias;
    const targetTable = (0, helpers_1.qualifyTable)(joinSpec.target.schema, joinSpec.target.table, context.defaultSchema);
    const bindings = [];
    const clauses = [
        (0, helpers_1.buildTargetCorrelationSql)(joinSpec, correlateSpec, targetAlias, linkType),
        (0, helpers_1.buildTargetSoftDeleteSql)(targetAlias, context.withDeleted),
    ];
    const targetFilters = getTargetFilters(joinSpec, targetAlias, context);
    clauses.push(...targetFilters.clauses);
    bindings.push(...targetFilters.bindings);
    let sql = `exists (select 1 from ${targetTable} as ${(0, helpers_1.quoteIdentifier)(targetAlias)} where ${clauses.filter(Boolean).join(" and ")})`;
    return { sql, bindings };
}
function getTargetFilters(joinSpec, targetAlias, context) {
    const targetPrimaryKey = (0, helpers_1.getTargetPrimaryKey)(joinSpec);
    const bindings = [];
    const clauses = [];
    if (hasTargetFilters(joinSpec)) {
        const targetFilters = buildFilterSql(targetAlias, joinSpec.target.filters);
        clauses.push(targetFilters.sql);
        bindings.push(...targetFilters.bindings);
    }
    const children = context.childrenByParent.get(joinSpec.alias) ?? [];
    for (const child of children) {
        if (!joinRequiresFilter(child, context.childrenByParent)) {
            continue;
        }
        const nested = buildExistsSql(child, {
            table: joinSpec.target.table,
            alias: targetAlias,
            key: targetPrimaryKey,
        }, context);
        if (nested.sql) {
            clauses.push(nested.sql);
            bindings.push(...nested.bindings);
        }
    }
    return { clauses, bindings };
}
function nextLinkAlias(context) {
    return `cm_link_${context.linkAliasCounter.value++}`;
}
function hasTargetFilters(joinSpec) {
    return (!!joinSpec.target.filters && Object.keys(joinSpec.target.filters).length > 0);
}
function buildOperatorSql(column, operator, value, bindings) {
    switch (operator) {
        case "$eq":
            if (value === null) {
                return `${column} is null`;
            }
            bindings.push(value);
            return `${column} = ?`;
        case "$ne":
            if (value === null) {
                return `${column} is not null`;
            }
            bindings.push(value);
            return `${column} <> ?`;
        case "$in": {
            const values = value ?? [];
            const inArraySql = buildInArraySql(column, values, false);
            if (!inArraySql) {
                return "1 = 0";
            }
            bindings.push(...inArraySql.bindings);
            return inArraySql.sql;
        }
        case "$nin": {
            const values = value ?? [];
            const inArraySql = buildInArraySql(column, values, true);
            if (!inArraySql) {
                return undefined;
            }
            bindings.push(...inArraySql.bindings);
            return inArraySql.sql;
        }
        case "$gt":
            bindings.push(value);
            return `${column} > ?`;
        case "$gte":
            bindings.push(value);
            return `${column} >= ?`;
        case "$lt":
            bindings.push(value);
            return `${column} < ?`;
        case "$lte":
            bindings.push(value);
            return `${column} <= ?`;
        case "$like":
            bindings.push(value);
            return `${column} like ?`;
        case "$ilike":
            bindings.push(value);
            return `${column} ilike ?`;
        case "$is":
            if (value === null) {
                return `${column} is null`;
            }
            bindings.push(value);
            return `${column} is ?`;
        default:
            throw new common_1.MedusaError(common_1.MedusaError.Types.INVALID_DATA, `Invalid operator: ${operator}`);
    }
}
function buildInArraySql(column, values, negated) {
    if (!values.length) {
        return negated ? undefined : { sql: "1 = 0", bindings: [] };
    }
    // Use `any(array[...])` so MikroORM's formatQuery inlines values as
    // `array['a','b']` instead of the invalid `any('a','b')` from a single array
    // binding with `any(?)`.
    const placeholders = values.map(() => "?").join(", ");
    const arraySql = `array[${placeholders}]`;
    const sql = negated
        ? `not (${column} = any(${arraySql}))`
        : `${column} = any(${arraySql})`;
    return {
        sql,
        bindings: values,
    };
}
//# sourceMappingURL=filter-sql.js.map