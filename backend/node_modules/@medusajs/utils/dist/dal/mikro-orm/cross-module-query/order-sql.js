"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transformOrderByForCrossModuleJoins = transformOrderByForCrossModuleJoins;
const core_1 = require("@medusajs/deps/mikro-orm/core");
const common_1 = require("../../../common");
const helpers_1 = require("./helpers");
function transformOrderByForCrossModuleJoins(orderBy, crossModuleJoins, correlateSpec, defaultSchema, withDeleted) {
    if (!orderBy) {
        return orderBy;
    }
    if (Array.isArray(orderBy)) {
        return orderBy.map((entry) => transformOrderByObject(entry, crossModuleJoins, correlateSpec, defaultSchema, withDeleted));
    }
    return transformOrderByObject(orderBy, crossModuleJoins, correlateSpec, defaultSchema, withDeleted);
}
function transformOrderByObject(orderBy, crossModuleJoins, correlateSpec, defaultSchema, withDeleted) {
    const transformed = {};
    for (const [key, direction] of Object.entries(orderBy)) {
        if (direction === "ASC" || direction === "DESC") {
            const [alias, ...rest] = key.split(".");
            const join = rest.length > 0
                ? getJoinSpecByAlias(crossModuleJoins, alias)
                : undefined;
            if (join) {
                transformed[buildOrderByScalarSubquery(join.joinSpec, correlateSpec, join.index, rest.join("."), defaultSchema, withDeleted)] = direction;
            }
            else {
                transformed[key] = direction;
            }
            continue;
        }
        if ((0, common_1.isObject)(direction)) {
            transformed[key] = transformOrderByObject(direction, crossModuleJoins, correlateSpec, defaultSchema, withDeleted);
        }
    }
    return transformed;
}
function buildOrderByScalarSubquery(joinSpec, correlateSpec, index, field, defaultSchema, withDeleted) {
    const linkType = (0, helpers_1.inferLinkType)(joinSpec, correlateSpec);
    const targetPrimaryKey = (0, helpers_1.getTargetPrimaryKey)(joinSpec);
    if (field === targetPrimaryKey && linkType === "pivot") {
        return buildTargetIdOnlyOrderSql(joinSpec, correlateSpec, index, defaultSchema, withDeleted);
    }
    if (linkType === "pivot") {
        return buildPivotLinkOrderSql(joinSpec, correlateSpec, index, field, defaultSchema, withDeleted);
    }
    else {
        return buildNoLinkOrderSql(joinSpec, correlateSpec, field, defaultSchema, withDeleted, linkType);
    }
}
function buildTargetIdOnlyOrderSql(joinSpec, correlateSpec, index, defaultSchema, withDeleted) {
    const linkAlias = `cm_order_link_${index}`;
    const linkTable = (0, helpers_1.qualifyTable)(joinSpec.link.schema, joinSpec.link.table, defaultSchema);
    const clauses = [
        (0, helpers_1.buildLinkCorrelationSql)(joinSpec, correlateSpec, linkAlias),
        (0, helpers_1.buildLinkSoftDeleteSql)(linkAlias, withDeleted),
    ].filter(Boolean);
    return (0, core_1.raw)(`(select ${(0, helpers_1.quoteIdentifier)(linkAlias)}.${(0, helpers_1.quoteIdentifier)(joinSpec.link.targetKey)} from ${linkTable} as ${(0, helpers_1.quoteIdentifier)(linkAlias)} where ${clauses.join(" and ")} order by ${(0, helpers_1.quoteIdentifier)(linkAlias)}.${(0, helpers_1.quoteIdentifier)(joinSpec.link.targetKey)} limit 1)`);
}
function buildPivotLinkOrderSql(joinSpec, correlateSpec, index, field, defaultSchema, withDeleted) {
    const linkAlias = `cm_order_link_${index}`;
    const linkTable = (0, helpers_1.qualifyTable)(joinSpec.link.schema, joinSpec.link.table, defaultSchema);
    const targetAlias = joinSpec.alias;
    const targetTable = (0, helpers_1.qualifyTable)(joinSpec.target.schema, joinSpec.target.table, defaultSchema);
    const targetPrimaryKey = (0, helpers_1.getTargetPrimaryKey)(joinSpec);
    const clauses = [
        (0, helpers_1.buildLinkCorrelationSql)(joinSpec, correlateSpec, linkAlias),
        (0, helpers_1.buildLinkToTargetJoinSql)(joinSpec, linkAlias, targetAlias),
        (0, helpers_1.buildLinkSoftDeleteSql)(linkAlias, withDeleted),
        (0, helpers_1.buildTargetSoftDeleteSql)(targetAlias, withDeleted),
    ];
    /*
     * Currently links always use a pivot table, even for 1-to-1 relationships, and the checks
     * on them are done in the application layer. Because of that, we essentially suport a to-many
     * sorting behavior, which is not ideal.
     */
    return (0, core_1.raw)(`(select ${(0, helpers_1.quoteIdentifier)(targetAlias)}.${(0, helpers_1.quoteIdentifier)(field)} from ${linkTable} as ${(0, helpers_1.quoteIdentifier)(linkAlias)} inner join ${targetTable} as ${(0, helpers_1.quoteIdentifier)(targetAlias)} on true where ${clauses.join(" and ")} order by ${(0, helpers_1.quoteIdentifier)(targetAlias)}.${(0, helpers_1.quoteIdentifier)(targetPrimaryKey)} limit 1)`);
}
function buildNoLinkOrderSql(joinSpec, correlateSpec, field, defaultSchema, withDeleted, linkType) {
    const targetAlias = joinSpec.alias;
    const targetTable = (0, helpers_1.qualifyTable)(joinSpec.target.schema, joinSpec.target.table, defaultSchema);
    const targetPrimaryKey = (0, helpers_1.getTargetPrimaryKey)(joinSpec);
    const clauses = [
        (0, helpers_1.buildTargetCorrelationSql)(joinSpec, correlateSpec, targetAlias, linkType),
        (0, helpers_1.buildTargetSoftDeleteSql)(targetAlias, withDeleted),
    ];
    return (0, core_1.raw)(`(select ${(0, helpers_1.quoteIdentifier)(targetAlias)}.${(0, helpers_1.quoteIdentifier)(field)} from ${targetTable} as ${(0, helpers_1.quoteIdentifier)(targetAlias)} where ${clauses.join(" and ")} order by ${(0, helpers_1.quoteIdentifier)(targetAlias)}.${(0, helpers_1.quoteIdentifier)(targetPrimaryKey)} limit 1)`);
}
function getJoinSpecByAlias(crossModuleJoins, alias) {
    const index = crossModuleJoins.findIndex((join) => join.alias === alias);
    if (index === -1) {
        return undefined;
    }
    return {
        joinSpec: crossModuleJoins[index],
        index,
    };
}
//# sourceMappingURL=order-sql.js.map