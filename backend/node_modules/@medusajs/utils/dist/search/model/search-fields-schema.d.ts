import { SearchFieldDefinition, SearchFieldsSchemaLike, SearchSchema } from "@medusajs/types";
declare const IsSearchFieldsSchema: unique symbol;
/**
 * A fields schema produced by `search.define(...)`. Compiles to plain
 * SearchFieldDefinition records for the Search Module.
 */
export declare class SearchFieldsSchema<Schema extends SearchSchema> implements SearchFieldsSchemaLike {
    [IsSearchFieldsSchema]: boolean;
    /**
     * The DSL schema. Public so document types can be inferred from it —
     * see InferSearchDocumentType.
     */
    schema: Schema;
    constructor(schema: Schema);
    static isSearchFieldsSchema(value: unknown): value is SearchFieldsSchema<any>;
    /**
     * Compile the DSL schema into plain SearchFieldDefinition records.
     */
    toFields(): Record<string, SearchFieldDefinition>;
}
export {};
//# sourceMappingURL=search-fields-schema.d.ts.map