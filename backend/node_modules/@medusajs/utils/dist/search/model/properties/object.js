"use strict";
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _ObjectProperty_correlated, _ObjectProperty_fields;
Object.defineProperty(exports, "__esModule", { value: true });
exports.ObjectProperty = void 0;
const base_1 = require("./base");
class ObjectProperty extends base_1.ScalarSearchProperty {
    constructor(fields) {
        super();
        this.dataType = {
            name: "object",
        };
        _ObjectProperty_correlated.set(this, void 0);
        _ObjectProperty_fields.set(this, void 0);
        __classPrivateFieldSet(this, _ObjectProperty_fields, fields, "f");
    }
    // Correlated filtering only means anything on an array of objects, so the
    // receiver must have gone through `.array()` first.
    correlated(value = true) {
        __classPrivateFieldSet(this, _ObjectProperty_correlated, value, "f");
        return this;
    }
    parse(fieldName) {
        const metadata = super.parse(fieldName);
        const fields = {};
        for (const [name, property] of Object.entries(__classPrivateFieldGet(this, _ObjectProperty_fields, "f"))) {
            fields[name] = property.parse(name);
        }
        if (__classPrivateFieldGet(this, _ObjectProperty_correlated, "f") !== undefined) {
            metadata.correlated = __classPrivateFieldGet(this, _ObjectProperty_correlated, "f");
        }
        metadata.fields = fields;
        return metadata;
    }
}
exports.ObjectProperty = ObjectProperty;
_ObjectProperty_correlated = new WeakMap(), _ObjectProperty_fields = new WeakMap();
//# sourceMappingURL=object.js.map