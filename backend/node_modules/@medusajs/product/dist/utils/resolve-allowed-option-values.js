"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveAllowedOptionValues = resolveAllowedOptionValues;
const utils_1 = require("@medusajs/framework/utils");
function resolveAllowedOptionValues({ optionTitle, value, optionValues, allowedValueIds, }) {
    const valueNameOf = (v) => v?.value?.value ?? v?.value;
    const idOf = (v) => v?.value?.id ?? v?.id;
    const candidateValues = allowedValueIds
        ? optionValues?.filter((v) => {
            const id = idOf(v);
            return id ? allowedValueIds.has(id) : true;
        })
        : optionValues;
    const match = candidateValues?.find((v) => valueNameOf(v) === value);
    if (!match) {
        throw new utils_1.MedusaError(utils_1.MedusaError.Types.INVALID_DATA, `Option value ${value} does not exist for option ${optionTitle}`);
    }
    return match;
}
//# sourceMappingURL=resolve-allowed-option-values.js.map