import { AuthTypes, Context, IAuthVerificationProvider, Logger } from "@medusajs/framework/types";
import { AuthVerificationProviderRegistrationPrefix } from "../types";
type InjectedDependencies = {
    logger?: Logger;
    [key: `${typeof AuthVerificationProviderRegistrationPrefix}${string}`]: IAuthVerificationProvider;
};
export default class AuthVerificationProviderService {
    #private;
    protected dependencies: InjectedDependencies;
    constructor(container: InjectedDependencies);
    protected retrieveProviderRegistration(provider: string): IAuthVerificationProvider;
    request(provider: string, data: AuthTypes.RequestAuthVerificationDTO, sharedContext?: Context): Promise<AuthTypes.RequestAuthVerificationResponse>;
    confirm(provider: string, data: AuthTypes.ConfirmAuthVerificationDTO, sharedContext?: Context): Promise<AuthTypes.ConfirmAuthVerificationResponse>;
}
export {};
//# sourceMappingURL=verification-provider.d.ts.map