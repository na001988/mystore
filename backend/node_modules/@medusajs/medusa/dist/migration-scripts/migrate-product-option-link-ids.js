"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = migrateProductOptionLinkIds;
const modules_sdk_1 = require("@medusajs/framework/modules-sdk");
const utils_1 = require("@medusajs/framework/utils");
const BATCH_SIZE = 1000;
async function updateIdsInBatches({ tableName, idPrefix, pgConnection, logger, }) {
    const idPrefixPattern = `${idPrefix}_%`;
    const { rows: countRows } = await pgConnection.raw(`select count(id)::int as count from "${tableName}" where id not like ?`, [idPrefixPattern]);
    const totalRows = Number(countRows[0]?.count ?? 0);
    const maxBatches = Math.ceil(totalRows / BATCH_SIZE);
    let batchCount = 0;
    let updatedRows = 0;
    if (!totalRows) {
        logger.info(`No ids to rewrite in ${tableName}. Skipping.`);
        return;
    }
    logger.info(`Rewriting ${totalRows} ids in ${tableName}...`);
    while (batchCount < maxBatches) {
        const { rows } = await pgConnection.raw(`select id from "${tableName}" where id not like ? order by id limit ?`, [idPrefixPattern, BATCH_SIZE]);
        if (!rows.length) {
            break;
        }
        batchCount += 1;
        const idMappings = rows.map((row) => ({
            oldId: row.id,
            newId: (0, utils_1.generateEntityId)(undefined, idPrefix),
        }));
        const valuesClause = idMappings.map(() => "(?, ?)").join(", ");
        const bindings = idMappings.flatMap((entry) => [entry.oldId, entry.newId]);
        await pgConnection.raw(`update "${tableName}" as t
        set id = v.new_id
        from (values ${valuesClause}) as v(old_id, new_id)
        where t.id = v.old_id`, bindings);
        updatedRows += rows.length;
        logger.info(`Rewrote ${updatedRows}/${totalRows} ids in ${tableName}.`);
    }
    logger.info(`Finished rewriting ${updatedRows} ids in ${tableName}.`);
}
async function migrateProductOptionLinkIds({ container, }) {
    if (!modules_sdk_1.MedusaModule.isInstalled(utils_1.Modules.PRODUCT)) {
        return;
    }
    const logger = container.resolve(utils_1.ContainerRegistrationKeys.LOGGER);
    const pgConnection = container.resolve(utils_1.ContainerRegistrationKeys.PG_CONNECTION);
    logger.info("Starting migration of product option link ids...");
    try {
        await pgConnection.transaction(async (trx) => {
            await updateIdsInBatches({
                tableName: "product_product_option",
                idPrefix: "prodopt",
                pgConnection: trx,
                logger,
            });
            await updateIdsInBatches({
                tableName: "product_product_option_value",
                idPrefix: "prodoptval",
                pgConnection: trx,
                logger,
            });
        });
        logger.info("Finished migration of product option link ids.");
    }
    catch (error) {
        logger.error(error);
        throw error;
    }
}
//# sourceMappingURL=migrate-product-option-link-ids.js.map