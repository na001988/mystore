"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.POST = void 0;
const utils_1 = require("@medusajs/framework/utils");
/**
 * @since 2.16.0
 */
const POST = async (req, res) => {
    const { code, code_provider } = req.validatedBody;
    const authService = req.scope.resolve(utils_1.Modules.AUTH);
    const result = await authService.confirmAuthVerification({
        auth_identity_id: "auth_context" in req ? req.auth_context.auth_identity_id : undefined,
        code,
        code_provider,
    });
    return res.status(200).json({
        entity_id: result.entity_id,
        entity_type: result.entity_type,
        code_provider: result.code_provider,
        verified_at: result.verified_at,
    });
};
exports.POST = POST;
//# sourceMappingURL=route.js.map