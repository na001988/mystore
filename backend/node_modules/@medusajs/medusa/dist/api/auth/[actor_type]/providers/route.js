"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GET = void 0;
const utils_1 = require("@medusajs/framework/utils");
const auth_methods_per_actor_1 = require("../../utils/auth-methods-per-actor");
const GET = async (req, res) => {
    const { actor_type } = req.params;
    const service = req.scope.resolve(utils_1.Modules.AUTH);
    const config = req.scope.resolve(utils_1.ContainerRegistrationKeys.CONFIG_MODULE);
    const allowedProviderIds = (0, auth_methods_per_actor_1.getAllowedAuthProvidersForActor)(config, actor_type);
    const providers = await service.listAuthProviders(allowedProviderIds ? { id: allowedProviderIds } : undefined);
    res.status(200).json({
        providers: providers.map((provider) => ({
            id: provider.id,
            identifier: provider.identifier,
            display_name: provider.display_name,
            flow: provider.flow,
        })),
    });
};
exports.GET = GET;
//# sourceMappingURL=route.js.map