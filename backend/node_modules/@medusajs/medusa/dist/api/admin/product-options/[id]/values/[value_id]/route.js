"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DELETE = exports.POST = exports.GET = void 0;
const core_flows_1 = require("@medusajs/core-flows");
const utils_1 = require("@medusajs/framework/utils");
const retrieveProductOptionValue = async (req, fields) => {
    // Ensure the value belongs to the option
    const { value_id: valueId, id: optionId } = req.params;
    const query = req.scope.resolve(utils_1.ContainerRegistrationKeys.QUERY);
    const { data: [product_option_value], } = await query.graph({
        entity: "product_option_value",
        filters: { id: valueId, option_id: optionId },
        fields: fields ?? ["id"],
    });
    if (!product_option_value) {
        throw new utils_1.MedusaError(utils_1.MedusaError.Types.NOT_FOUND, `Product option value with id "${valueId}" was not found for option with id "${optionId}"`);
    }
    return product_option_value;
};
/**
 * @since 2.16.0
 */
const GET = async (req, res) => {
    const product_option_value = await retrieveProductOptionValue(req, req.queryConfig.fields);
    res.status(200).json({ product_option_value });
};
exports.GET = GET;
/**
 * @since 2.17.0
 */
const POST = async (req, res) => {
    // Ensure the value belongs to the option in the route before updating.
    const query = req.scope.resolve(utils_1.ContainerRegistrationKeys.QUERY);
    const { value_id: valueId } = req.params;
    await retrieveProductOptionValue(req);
    await (0, core_flows_1.updateProductOptionValuesWorkflow)(req.scope).run({
        input: {
            id: req.params.value_id,
            update: req.validatedBody,
        },
    });
    const { data: [updatedProductOptionValue], } = await query.graph({
        entity: "product_option_value",
        filters: { id: valueId },
        fields: req.queryConfig.fields,
    });
    res.status(200).json({ product_option_value: updatedProductOptionValue });
};
exports.POST = POST;
/**
 * @since 2.17.0
 */
const DELETE = async (req, res) => {
    const { value_id: valueId } = req.params;
    // Ensure the value belongs to the option in the route before deleting.
    await retrieveProductOptionValue(req);
    await (0, core_flows_1.deleteProductOptionValuesWorkflow)(req.scope).run({
        input: { ids: [valueId] },
    });
    res.status(200).json({
        id: valueId,
        object: "product_option_value",
        deleted: true,
    });
};
exports.DELETE = DELETE;
//# sourceMappingURL=route.js.map