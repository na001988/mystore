"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.searchWithGraphFallback = searchWithGraphFallback;
const utils_1 = require("@medusajs/framework/utils");
const search_entities_1 = require("./search-entities");
/**
 * Fans out free-text `q` queries across the admin search entity registry via
 * `query.graph`. Used when the Search Module is not registered.
 */
async function searchWithGraphFallback(scope, { q, entity, skip, take }) {
    const names = entity?.length ? entity : search_entities_1.DEFAULT_ADMIN_SEARCH_ENTITY_NAMES;
    const configs = names.map((name) => {
        const config = search_entities_1.ADMIN_SEARCH_ENTITY_MAP.get(name);
        if (!config) {
            throw new utils_1.MedusaError(utils_1.MedusaError.Types.NOT_FOUND, `No search entity registered for "${name}". Registered entities: ${search_entities_1.DEFAULT_ADMIN_SEARCH_ENTITY_NAMES.join(", ")}`);
        }
        return config;
    });
    const query = scope.resolve(utils_1.ContainerRegistrationKeys.QUERY);
    return await (0, utils_1.promiseAll)(configs.map(async (config) => {
        const filters = {
            ...(config.filters ?? {}),
        };
        if (q) {
            filters.q = config.transformQuery ? config.transformQuery(q) : q;
        }
        const { data, metadata } = await query.graph({
            entity: config.graphEntity,
            fields: config.fields,
            filters,
            pagination: { skip, take },
        });
        return {
            entity: config.name,
            data: data,
            count: metadata?.count ?? 0,
            offset: metadata?.skip ?? skip,
            limit: metadata?.take ?? take,
        };
    }));
}
//# sourceMappingURL=fallback-search.js.map