import { HttpTypes, MedusaContainer } from "@medusajs/framework/types";
type FallbackSearchInput = {
    q?: string;
    entity?: string[];
    skip: number;
    take: number;
};
/**
 * Fans out free-text `q` queries across the admin search entity registry via
 * `query.graph`. Used when the Search Module is not registered.
 */
export declare function searchWithGraphFallback(scope: MedusaContainer, { q, entity, skip, take }: FallbackSearchInput): Promise<HttpTypes.AdminSearchResultGroup[]>;
export {};
//# sourceMappingURL=fallback-search.d.ts.map