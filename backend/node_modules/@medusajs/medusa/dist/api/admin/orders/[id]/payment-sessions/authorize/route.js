"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.POST = void 0;
const core_flows_1 = require("@medusajs/core-flows");
/**
 * @since 2.17.0
 */
const POST = async (req, res) => {
    const { id } = req.params;
    const { payment_session_id } = req.validatedBody;
    const { result: payment } = await (0, core_flows_1.authorizePaymentSessionForOrderWorkflow)(req.scope).run({
        input: { payment_session_id },
    });
    const order = await (0, core_flows_1.getOrderDetailWorkflow)(req.scope).run({
        input: { order_id: id, fields: req.queryConfig.fields },
    });
    res.status(200).json({
        order: order.result,
        is_authorized: !!payment,
    });
};
exports.POST = POST;
//# sourceMappingURL=route.js.map