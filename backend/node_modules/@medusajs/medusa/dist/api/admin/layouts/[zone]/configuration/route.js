"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DELETE = exports.POST = exports.GET = void 0;
const utils_1 = require("@medusajs/framework/utils");
const core_flows_1 = require("@medusajs/core-flows");
const getConfigurations = async (settingsService, zone, userId) => {
    const [personal] = await settingsService.listLayoutConfigurations({ zone, user_id: userId }, { take: 1 });
    const defaultConfiguration = await settingsService.getSystemDefaultLayoutConfiguration(zone);
    const storedScope = await settingsService.getActiveLayoutScope(zone, userId);
    // Resolve the scope to show: the user's persisted choice when still valid,
    // otherwise their personal view if they have one, else the default.
    let activeScope;
    if (storedScope === "personal" && personal) {
        activeScope = "personal";
    }
    else if (storedScope === "default") {
        activeScope = "default";
    }
    else {
        activeScope = personal ? "personal" : "default";
    }
    return {
        personal_configuration: personal ?? null,
        default_configuration: defaultConfiguration,
        active_scope: activeScope,
    };
};
/**
 * @since 2.17.2
 */
const GET = async (req, res) => {
    const settingsService = req.scope.resolve(utils_1.Modules.SETTINGS);
    res.json(await getConfigurations(settingsService, req.params.zone, req.auth_context.actor_id));
};
exports.GET = GET;
/**
 * @since 2.17.2
 */
const POST = async (req, res) => {
    await (0, core_flows_1.setLayoutConfigurationWorkflow)(req.scope).run({
        input: {
            zone: req.params.zone,
            user_id: req.auth_context.actor_id,
            is_default: req.body.is_default,
            configuration: req.body.configuration,
        },
    });
    const settingsService = req.scope.resolve(utils_1.Modules.SETTINGS);
    res.json(await getConfigurations(settingsService, req.params.zone, req.auth_context.actor_id));
};
exports.POST = POST;
/**
 * @since 2.17.2
 */
const DELETE = async (req, res) => {
    await (0, core_flows_1.clearLayoutConfigurationWorkflow)(req.scope).run({
        input: {
            zone: req.params.zone,
            user_id: req.auth_context.actor_id,
        },
    });
    res.json({ success: true });
};
exports.DELETE = DELETE;
//# sourceMappingURL=route.js.map