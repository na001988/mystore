"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.migrate = migrate;
const framework_1 = require("@medusajs/framework");
const links_1 = require("@medusajs/framework/links");
const utils_1 = require("@medusajs/framework/utils");
const child_process_1 = require("child_process");
const path_1 = __importStar(require("path"));
const loaders_1 = require("../../loaders");
const search_1 = require("../../loaders/search");
const utils_2 = require("../utils");
const sync_links_1 = require("./sync-links");
const TERMINAL_SIZE = process.stdout.columns;
const cliPath = path_1.default.resolve(framework_1.MEDUSA_CLI_PATH, "..", "..", "cli.js");
/**
 * A low-level utility to migrate the database. This util should
 * never exit the process implicitly.
 */
async function migrate({ directory, skipLinks, skipScripts, skipSearch, executeAllLinks, executeSafeLinks, allOrNothing, concurrency, logger, container, }) {
    /**
     * Setup
     */
    await (0, utils_2.ensureDbExists)(container);
    // If pgstream is enabled, force concurrency to 1
    const pgstreamEnabled = await (0, utils_2.isPgstreamEnabled)(container);
    if (pgstreamEnabled) {
        concurrency = 1;
    }
    if ((0, utils_1.isDefined)(concurrency)) {
        process.env.DB_MIGRATION_CONCURRENCY = String(concurrency);
    }
    const medusaAppLoader = new framework_1.MedusaAppLoader();
    const configModule = container.resolve(utils_1.ContainerRegistrationKeys.CONFIG_MODULE);
    const plugins = await (0, utils_1.getResolvedPlugins)(directory, configModule, true);
    (0, utils_1.mergePluginModules)(configModule, plugins, directory);
    const linksSourcePaths = plugins.map((plugin) => (0, path_1.join)(plugin.resolve, "links"));
    await new links_1.LinkLoader(linksSourcePaths, logger).load();
    /**
     * Run migrations
     */
    logger.info("Running migrations...");
    const migrator = new framework_1.Migrator({ container });
    await migrator.ensureMigrationsTable();
    await medusaAppLoader.runModulesMigrations({
        action: "run",
        allOrNothing,
    });
    logger.log(new Array(TERMINAL_SIZE).join("-"));
    logger.info("Migrations completed");
    /**
     * Sync links
     */
    if (!skipLinks) {
        logger.log(new Array(TERMINAL_SIZE).join("-"));
        await (0, sync_links_1.syncLinks)(medusaAppLoader, {
            executeAll: executeAllLinks,
            executeSafe: executeSafeLinks,
            directory,
            container,
            concurrency,
        });
    }
    /**
     * Create and alter search indexes
     *
     * Runs in a child process because it needs a fully loaded app — this one only
     * ever loads modules far enough to plan migrations — and before the migration
     * scripts, whose boot is the first thing that can seed what this creates.
     */
    if (!skipSearch && (0, search_1.isSearchModuleEnabled)(configModule)) {
        const exitCode = await runCliCommand("db:migrate:search", directory);
        // Reported rather than swallowed: the seed at application start cannot tell a
        // half-migrated index from a fresh one, so this has to be seen now.
        if (exitCode !== 0) {
            return false;
        }
    }
    if (!skipScripts) {
        /**
         * Run migration scripts
         */
        logger.log(new Array(TERMINAL_SIZE).join("-"));
        await runCliCommand("db:migrate:scripts", directory);
    }
    return true;
}
async function runCliCommand(command, directory) {
    const childProcess = (0, child_process_1.fork)(cliPath, [command], {
        cwd: directory,
        env: process.env,
    });
    return await new Promise((resolve, reject) => {
        childProcess.on("error", (error) => {
            reject(error);
        });
        // A signal leaves the code null, which is not a success either.
        childProcess.on("close", (code) => {
            resolve(code ?? 1);
        });
    });
}
const main = async function ({ directory, skipLinks, skipScripts, skipSearch, executeAllLinks, executeSafeLinks, concurrency, allOrNothing, }) {
    process.env.MEDUSA_WORKER_MODE = "server";
    let logger;
    try {
        const container = await (0, loaders_1.initializeContainer)(directory);
        logger = container.resolve(utils_1.ContainerRegistrationKeys.LOGGER);
        const migrated = await migrate({
            directory,
            skipLinks,
            skipScripts,
            skipSearch,
            executeAllLinks,
            executeSafeLinks,
            concurrency,
            allOrNothing,
            logger,
            container,
        });
        process.exit(migrated ? 0 : 1);
    }
    catch (error) {
        if (logger) {
            logger.error(error);
        }
        else {
            console.error(error);
        }
        process.exit(1);
    }
};
exports.default = main;
//# sourceMappingURL=migrate.js.map