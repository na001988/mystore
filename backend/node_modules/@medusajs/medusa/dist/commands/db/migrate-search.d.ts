import { Logger, MedusaContainer } from "@medusajs/framework/types";
/**
 * Low-level utility to bring the physical search indexes in line with the loaded
 * definitions. Creates and alters them only — filling them is the seed that runs
 * at application start.
 */
export declare function migrateSearchIndexes({ directory, container, logger, }: {
    directory: string;
    container: MedusaContainer;
    logger: Logger;
}): Promise<boolean>;
declare const main: ({ directory }: {
    directory: string;
}) => Promise<never>;
export default main;
//# sourceMappingURL=migrate-search.d.ts.map