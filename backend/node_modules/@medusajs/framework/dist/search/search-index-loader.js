"use strict";
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _SearchIndexLoader_sourceDir, _SearchIndexLoader_excludes, _SearchIndexLoader_logger;
Object.defineProperty(exports, "__esModule", { value: true });
exports.SearchIndexLoader = void 0;
const utils_1 = require("@medusajs/utils");
const promises_1 = require("fs/promises");
const path_1 = require("path");
const logger_1 = require("../logger");
class SearchIndexLoader {
    constructor(sourceDir, logger) {
        /**
         * The directory from which to load the search index definitions
         * @private
         */
        _SearchIndexLoader_sourceDir.set(this, void 0);
        /**
         * The list of file names to exclude from the search index scan
         * @private
         */
        _SearchIndexLoader_excludes.set(this, [
            // Anchored, unlike the link loader's: `product-index.ts` is a natural name
            // for a file in `search/` and must not be skipped as a barrel file.
            /^index\.(js|ts)$/,
            /\.DS_Store/,
            /(\.ts\.map|\.js\.map|\.d\.ts|\.md)$/,
            /^_[^/\\]*(\.[^/\\]+)?$/,
        ]);
        _SearchIndexLoader_logger.set(this, void 0);
        __classPrivateFieldSet(this, _SearchIndexLoader_sourceDir, sourceDir, "f");
        __classPrivateFieldSet(this, _SearchIndexLoader_logger, logger ?? logger_1.logger, "f");
    }
    /**
     * Load the search index definitions from the source paths. `defineSearchIndex`
     * registers them, so importing the files is all there is to do. What was
     * registered reaches the Search Module as its `indexes` option when the app
     * boots, which is why this has to run before `MedusaApp`.
     */
    async load() {
        const normalizedSourcePath = Array.isArray(__classPrivateFieldGet(this, _SearchIndexLoader_sourceDir, "f"))
            ? __classPrivateFieldGet(this, _SearchIndexLoader_sourceDir, "f")
            : [__classPrivateFieldGet(this, _SearchIndexLoader_sourceDir, "f")];
        const promises = normalizedSourcePath.map(async (sourcePath) => {
            try {
                await (0, promises_1.access)(sourcePath);
            }
            catch {
                __classPrivateFieldGet(this, _SearchIndexLoader_logger, "f").debug(`No search index to load from ${sourcePath}. skipped.`);
                return;
            }
            return await (0, utils_1.readDirRecursive)(sourcePath).then(async (entries) => {
                const fileEntries = entries.filter((entry) => {
                    return (!entry.isDirectory() &&
                        !__classPrivateFieldGet(this, _SearchIndexLoader_excludes, "f").some((exclude) => exclude.test(entry.name)));
                });
                __classPrivateFieldGet(this, _SearchIndexLoader_logger, "f").debug(`Registering search indexes from ${sourcePath}.`);
                return await (0, utils_1.promiseAll)(fileEntries.map(async (entry) => {
                    const fullPath = (0, path_1.join)(entry.path, entry.name);
                    return await (0, utils_1.dynamicImport)(fullPath);
                }));
            });
        });
        await (0, utils_1.promiseAll)(promises);
        __classPrivateFieldGet(this, _SearchIndexLoader_logger, "f").debug(`Search indexes registered.`);
    }
}
exports.SearchIndexLoader = SearchIndexLoader;
_SearchIndexLoader_sourceDir = new WeakMap(), _SearchIndexLoader_excludes = new WeakMap(), _SearchIndexLoader_logger = new WeakMap();
//# sourceMappingURL=search-index-loader.js.map