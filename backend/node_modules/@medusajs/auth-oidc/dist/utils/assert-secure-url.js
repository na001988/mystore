"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.assertSecureUrl = void 0;
const utils_1 = require("@medusajs/framework/utils");
/**
 * Asserts that a URL is `https`. Outside of production, `http` is allowed for
 * localhost so local development and testing remain possible.
 */
const assertSecureUrl = (value, label) => {
    let url;
    try {
        url = new URL(value);
    }
    catch (e) {
        throw new utils_1.MedusaError(utils_1.MedusaError.Types.INVALID_DATA, `OIDC '${label}' must be a valid URL`);
    }
    const isLocalhost = url.hostname === "localhost" ||
        url.hostname === "127.0.0.1" ||
        url.hostname === "::1" ||
        url.hostname === "[::1]";
    const allowsHttp = isLocalhost && !(0, utils_1.isProduction)();
    if (url.protocol !== "https:" && !(url.protocol === "http:" && allowsHttp)) {
        throw new utils_1.MedusaError(utils_1.MedusaError.Types.INVALID_DATA, `OIDC '${label}' must use https (http is only allowed for localhost outside of production)`);
    }
};
exports.assertSecureUrl = assertSecureUrl;
//# sourceMappingURL=assert-secure-url.js.map