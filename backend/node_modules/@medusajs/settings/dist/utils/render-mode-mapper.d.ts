/**
 * Render mode mapping utilities for column generation.
 * Maps field names and GraphQL types to appropriate render modes.
 */
import { GraphQLNamedType } from "@medusajs/framework/utils";
/**
 * Common render mode type that can be extended by plugins.
 */
export type RenderMode = "text" | "number" | "currency" | "date" | "datetime" | "boolean" | "status" | "badges" | "count" | "id" | "display_id" | "email" | "phone" | "url" | "image" | "json" | "country_code" | "address" | "name" | "product_info" | (string & {});
/**
 * Known filter operators. Mirrors the operators the generator emits per data
 * type in {@link ./filter-rules.ts}.
 */
export type FilterOperator = "eq" | "ne" | "gt" | "gte" | "lt" | "lte" | "contains" | "startsWith" | "endsWith" | "in" | "nin" | (string & {});
/**
 * Known computed column categories used for grouping in the UI.
 */
export type ColumnCategory = "relationship" | "metadata" | "computed" | "metric" | (string & {});
/**
 * Infer the render mode from a field name and GraphQL type.
 */
export declare function inferRenderMode(fieldName: string, graphqlType: GraphQLNamedType): {
    renderMode: RenderMode;
    semanticType: string;
};
/**
 * Column data type as used in AdminColumn.
 */
export type ColumnDataType = "string" | "number" | "boolean" | "date" | "currency" | "enum" | "object" | (string & {});
/**
 * Map a data type to its default render mode.
 */
export declare function dataTypeToRenderMode(dataType: ColumnDataType): RenderMode;
/**
 * Infer the data type from a GraphQL scalar type name.
 */
export declare function inferDataType(graphqlType: GraphQLNamedType, fieldName: string): ColumnDataType;
//# sourceMappingURL=render-mode-mapper.d.ts.map