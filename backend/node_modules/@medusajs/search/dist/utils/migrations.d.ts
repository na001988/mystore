import { SearchTypes } from "@medusajs/framework/types";
import { SearchIndexRegistry } from "../types";
/**
 * The index a schema change is built into. Derived from the definition hash rather
 * than a job id, so a migration and the boot that finishes it agree on the name
 * without persisting it, and an interrupted build is resumed rather than orphaned.
 */
export declare function shadowIndexName(definition: SearchTypes.ResolvedSearchIndexDefinition): string;
export declare function createIndexMigrationPlan(context: SearchIndexRegistry): Promise<SearchTypes.SearchIndexMigrationAction[]>;
export declare function executeIndexMigrationPlan(context: SearchIndexRegistry, actions: SearchTypes.SearchIndexMigrationAction[]): Promise<void>;
//# sourceMappingURL=migrations.d.ts.map