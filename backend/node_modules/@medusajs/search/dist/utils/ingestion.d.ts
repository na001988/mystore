import { Event, SearchTypes } from "@medusajs/framework/types";
import { SearchEventRoutes, SearchIndexes, SearchIngestionRuntime } from "../types";
export declare function buildEventRoutes(indexes: SearchIndexes): SearchEventRoutes;
/**
 * Turns one event into writes. Every write is waited on as far as its provider allows,
 * so awaiting this is an acknowledgement.
 */
export declare function ingestEvent(context: SearchIngestionRuntime, { event, routes }: {
    event: Event<any>;
    routes: SearchEventRoutes;
}): Promise<SearchTypes.SearchTask[]>;
//# sourceMappingURL=ingestion.d.ts.map