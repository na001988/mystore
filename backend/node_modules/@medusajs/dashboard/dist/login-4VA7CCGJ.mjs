import {
  AvatarBox
} from "./chunk-O333RR6K.mjs";
import "./chunk-KIIT4BNH.mjs";
import {
  isFetchError
} from "./chunk-ONB3JEHR.mjs";
import "./chunk-BPIG7PI6.mjs";
import {
  useExtension
} from "./chunk-5SZFF255.mjs";
import {
  Form
} from "./chunk-OBQI23QM.mjs";
import {
  callbackWithCloudAuth,
  useCloudAuthEnabled,
  useCreateCloudAuthUser,
  useVerifyAuthMfaChallenge
} from "./chunk-N3GR2PMX.mjs";
import "./chunk-HIX2NSSN.mjs";
import {
  provisionAuthUser,
  useAuthProviders,
  useSignInWithEmailPass
} from "./chunk-CXRGHH7Y.mjs";
import "./chunk-2LVQXUFY.mjs";
import "./chunk-EYDZJ522.mjs";
import "./chunk-QG545K2O.mjs";
import "./chunk-ESUUKILS.mjs";
import "./chunk-ISGDOD5J.mjs";
import "./chunk-QIUJGXDT.mjs";
import "./chunk-DEOCXBV2.mjs";
import "./chunk-ZB3WPQQA.mjs";
import "./chunk-2V5DOTI3.mjs";
import "./chunk-PTP3K7TB.mjs";
import "./chunk-EGZR6JFL.mjs";
import "./chunk-F7FNJYFK.mjs";
import "./chunk-TSAXQTUR.mjs";
import "./chunk-6OFSUHM5.mjs";
import "./chunk-3C2RPYDJ.mjs";
import "./chunk-4SIZ37QP.mjs";
import "./chunk-FGXPDJTN.mjs";
import "./chunk-RVOCY7U2.mjs";
import "./chunk-BGQF2VTH.mjs";
import "./chunk-6JFZX3SN.mjs";
import "./chunk-EMDIIWVL.mjs";
import "./chunk-YDJ774GR.mjs";
import "./chunk-SKQPG6BC.mjs";
import "./chunk-P3HKNKRE.mjs";
import "./chunk-D3TDNKSZ.mjs";
import "./chunk-HEAJ6KS5.mjs";
import "./chunk-7AXHHXCX.mjs";
import "./chunk-ACQJSQ5A.mjs";
import "./chunk-SEMVMECK.mjs";
import {
  sdk
} from "./chunk-NFEK63OE.mjs";
import "./chunk-QZ7TP4HQ.mjs";

// src/routes/login/login.tsx
import { Spinner as Spinner3 } from "@medusajs/icons";
import { Heading as Heading2, InlineTip, Text as Text3 } from "@medusajs/ui";
import { useMemo as useMemo2, useState as useState4 } from "react";
import { Trans, useTranslation as useTranslation5 } from "react-i18next";
import { Link as Link2, useLocation as useLocation2, useNavigate as useNavigate4 } from "react-router-dom";

// src/routes/login/components/cloud-auth-login.tsx
import { Spinner } from "@medusajs/icons";
import { Button, toast } from "@medusajs/ui";
import { useCallback, useEffect, useRef, useState } from "react";
import { useTranslation } from "react-i18next";
import { decodeToken } from "react-jwt";
import { useNavigate, useSearchParams } from "react-router-dom";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
var CLOUD_AUTH_PROVIDER = "cloud";
var CloudAuthLogin = ({ onMfaChallenge }) => {
  const { t } = useTranslation();
  const [searchParams] = useSearchParams();
  const { data: cloudAuth } = useCloudAuthEnabled();
  const isAutoLogin = searchParams.get("auth_provider") === CLOUD_AUTH_PROVIDER && searchParams.get("auto") === "true";
  const isCallback = searchParams.get("auth_provider") === CLOUD_AUTH_PROVIDER && (searchParams.has("code") || searchParams.has("error"));
  const { handleLogin, isLoginPending } = useHandleLogin(isAutoLogin);
  const { handleCallback, isCallbackPending } = useAuthCallback(
    searchParams,
    onMfaChallenge
  );
  const actionInitiated = useRef(false);
  useEffect(() => {
    if (actionInitiated.current) {
      return;
    }
    if (isAutoLogin) {
      actionInitiated.current = true;
      handleLogin();
    } else if (isCallback) {
      actionInitiated.current = true;
      handleCallback();
    }
  }, [isAutoLogin, isCallback, handleLogin, handleCallback]);
  if (isAutoLogin || isCallback) {
    return /* @__PURE__ */ jsx("div", { className: "bg-ui-bg-subtle fixed inset-0 z-50 flex items-center justify-center", children: /* @__PURE__ */ jsx(Spinner, { className: "text-ui-fg-subtle animate-spin" }) });
  }
  if (!cloudAuth?.enabled) {
    return null;
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx("hr", { className: "bg-ui-border-base my-4" }),
    /* @__PURE__ */ jsx(
      Button,
      {
        variant: "secondary",
        onClick: handleLogin,
        className: "w-full",
        disabled: isLoginPending || isCallbackPending,
        isLoading: isLoginPending || isCallbackPending,
        children: t("auth.login.cloud")
      }
    )
  ] });
};
var useHandleLogin = (isAutoLogin) => {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const [isPending, setIsPending] = useState(false);
  const handleLogin = useCallback(async () => {
    setIsPending(true);
    try {
      const result = await sdk.auth.login("user", CLOUD_AUTH_PROVIDER, {
        // setting callback_url in case the admin is on a different domain, or the backend URL is set to just "/" which won't work for the callback
        callback_url: `${window.location.origin}${window.location.pathname}?auth_provider=${CLOUD_AUTH_PROVIDER}`
      });
      if (typeof result === "object" && "location" in result) {
        window.location.href = result.location;
        return;
      }
      throw new Error("Unexpected login response");
    } catch {
      toast.error(t("auth.login.authenticationFailed"));
      if (isAutoLogin) {
        navigate("/login");
      }
    }
    setIsPending(false);
  }, [t, navigate, isAutoLogin]);
  return { handleLogin, isLoginPending: isPending };
};
var useAuthCallback = (searchParams, onMfaChallenge) => {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const { mutateAsync: createCloudAuthUser } = useCreateCloudAuthUser();
  const [isPending, setIsPending] = useState(false);
  const ensureCloudAuthUser = useCallback(
    async (token) => {
      const decodedToken = decodeToken(token);
      if (!decodedToken?.actor_id) {
        await createCloudAuthUser();
        const refreshedToken = await sdk.auth.refresh({
          Authorization: `Bearer ${token}`
          // passing it manually in case the auth type is session
        });
        if (!refreshedToken) {
          throw new Error("Failed to refresh token after user creation");
        }
      }
    },
    [createCloudAuthUser]
  );
  const handleCallback = useCallback(async () => {
    setIsPending(true);
    try {
      let token;
      try {
        const query = Object.fromEntries(searchParams);
        delete query.auth_provider;
        const result = await callbackWithCloudAuth(query);
        if (typeof result === "object" && "mfa_challenge" in result) {
          if (!onMfaChallenge) {
            throw new Error("MFA challenge handler is missing");
          }
          onMfaChallenge?.(result.mfa_challenge, async (verifiedToken) => {
            await ensureCloudAuthUser(verifiedToken);
            navigate("/");
          });
          return;
        }
        if (typeof result === "object" && "verification_required" in result) {
          throw new Error("Verification required but not implemented yet");
        }
        token = result;
      } catch (error) {
        throw new Error("Authentication callback failed");
      }
      await ensureCloudAuthUser(token);
      navigate("/");
    } catch (error) {
      toast.error(t("auth.login.authenticationFailed"));
      navigate("/login");
    }
    setIsPending(false);
  }, [searchParams, t, onMfaChallenge, ensureCloudAuthUser, navigate]);
  return { handleCallback, isCallbackPending: isPending };
};

// src/routes/login/components/email-pass-login.tsx
import { zodResolver } from "@hookform/resolvers/zod";
import { Alert, Button as Button2, Hint, Input } from "@medusajs/ui";
import { useForm } from "react-hook-form";
import { useTranslation as useTranslation2 } from "react-i18next";
import { useLocation, useNavigate as useNavigate2 } from "react-router-dom";
import * as z from "zod";
import { jsx as jsx2, jsxs as jsxs2 } from "react/jsx-runtime";
var EmailPassSchema = z.object({
  email: z.string().email(),
  password: z.string()
});
var EmailPassLogin = ({ onMfaChallenge }) => {
  const { t } = useTranslation2();
  const location = useLocation();
  const navigate = useNavigate2();
  const from = location.state?.from?.pathname || "/orders";
  const form = useForm({
    resolver: zodResolver(EmailPassSchema),
    defaultValues: {
      email: "",
      password: ""
    }
  });
  const { mutateAsync, isPending } = useSignInWithEmailPass();
  const handleSubmit = form.handleSubmit(async ({ email, password }) => {
    await mutateAsync(
      {
        email,
        password
      },
      {
        onError: (error) => {
          if (isFetchError(error)) {
            if (error.status === 401) {
              form.setError("email", {
                type: "manual",
                message: error.message
              });
              return;
            }
          }
          form.setError("root.serverError", {
            type: "manual",
            message: error.message
          });
        },
        onSuccess: (result) => {
          if (typeof result === "object" && "mfa_challenge" in result) {
            onMfaChallenge?.(result.mfa_challenge, () => {
              navigate(from, { replace: true });
            });
            return;
          }
          navigate(from, { replace: true });
        }
      }
    );
  });
  const serverError = form.formState.errors?.root?.serverError?.message;
  const validationError = form.formState.errors.email?.message || form.formState.errors.password?.message;
  return /* @__PURE__ */ jsx2(Form, { ...form, children: /* @__PURE__ */ jsxs2("form", { onSubmit: handleSubmit, className: "flex w-full flex-col gap-y-6", children: [
    /* @__PURE__ */ jsxs2("div", { className: "flex flex-col gap-y-1", children: [
      /* @__PURE__ */ jsx2(
        Form.Field,
        {
          control: form.control,
          name: "email",
          render: ({ field }) => {
            return /* @__PURE__ */ jsx2(Form.Item, { children: /* @__PURE__ */ jsx2(Form.Control, { children: /* @__PURE__ */ jsx2(
              Input,
              {
                autoComplete: "email",
                ...field,
                className: "bg-ui-bg-field-component",
                placeholder: t("fields.email")
              }
            ) }) });
          }
        }
      ),
      /* @__PURE__ */ jsx2(
        Form.Field,
        {
          control: form.control,
          name: "password",
          render: ({ field }) => {
            return /* @__PURE__ */ jsxs2(Form.Item, { children: [
              /* @__PURE__ */ jsx2(Form.Label, {}),
              /* @__PURE__ */ jsx2(Form.Control, { children: /* @__PURE__ */ jsx2(
                Input,
                {
                  type: "password",
                  autoComplete: "current-password",
                  ...field,
                  className: "bg-ui-bg-field-component",
                  placeholder: t("fields.password")
                }
              ) })
            ] });
          }
        }
      )
    ] }),
    validationError && /* @__PURE__ */ jsx2("div", { className: "text-center", children: /* @__PURE__ */ jsx2(Hint, { className: "inline-flex", variant: "error", children: validationError }) }),
    serverError && /* @__PURE__ */ jsx2(
      Alert,
      {
        className: "bg-ui-bg-base items-center p-2",
        dismissible: true,
        variant: "error",
        children: serverError
      }
    ),
    /* @__PURE__ */ jsx2(Button2, { className: "w-full", type: "submit", isLoading: isPending, children: t("actions.continueWithEmail") })
  ] }) });
};

// src/routes/login/components/mfa-challenge-form.tsx
import { Button as Button3, Heading, Hint as Hint2, Input as Input2, OtpInput, Text } from "@medusajs/ui";
import { useState as useState2 } from "react";
import { useTranslation as useTranslation3 } from "react-i18next";
import { Link } from "react-router-dom";
import { jsx as jsx3, jsxs as jsxs3 } from "react/jsx-runtime";
var getDefaultMethod = (methods) => {
  if (methods.includes("totp")) {
    return "totp";
  }
  return methods[0] ?? "totp";
};
var MfaChallengeForm = ({
  challenge,
  onSuccess,
  onBack
}) => {
  const { t } = useTranslation3();
  const [method, setMethod] = useState2(
    getDefaultMethod(challenge.methods)
  );
  const [code, setCode] = useState2("");
  const [error, setError] = useState2(null);
  const [verifiedToken, setVerifiedToken] = useState2(null);
  const [isCompleting, setIsCompleting] = useState2(false);
  const { mutateAsync, isPending } = useVerifyAuthMfaChallenge();
  const isRecoveryCode = method === "recovery_code";
  const canUseRecoveryCode = challenge.methods.includes("recovery_code");
  const canVerify = isRecoveryCode ? !!code.trim() : code.length === 6;
  const isLoading = isPending || isCompleting;
  const handleVerify = async (nextCode = code) => {
    const verificationCode = nextCode.trim();
    if (!verifiedToken && (!verificationCode || !isRecoveryCode && verificationCode.length !== 6)) {
      return;
    }
    setError(null);
    let token = verifiedToken;
    if (!token) {
      try {
        token = await mutateAsync({
          id: challenge.id,
          method,
          code: verificationCode
        });
        setVerifiedToken(token);
      } catch (e) {
        setError(e instanceof Error ? e.message : t("login.mfa.verifyError"));
        setCode("");
        return;
      }
    }
    setIsCompleting(true);
    try {
      await onSuccess(token);
    } catch (e) {
      setError(e instanceof Error ? e.message : t("login.mfa.completeError"));
    } finally {
      setIsCompleting(false);
    }
  };
  const handleOtpChange = (value) => {
    setCode(value);
  };
  const handleMethodChange = (nextMethod) => {
    setMethod(nextMethod);
    setCode("");
    setError(null);
    setVerifiedToken(null);
  };
  return /* @__PURE__ */ jsxs3("div", { className: "flex w-full flex-col items-center", children: [
    /* @__PURE__ */ jsxs3("div", { className: "mb-6 flex flex-col items-center", children: [
      /* @__PURE__ */ jsx3(Heading, { children: t("login.mfa.title") }),
      /* @__PURE__ */ jsx3(Text, { size: "small", className: "text-ui-fg-subtle text-center", children: isRecoveryCode ? t("login.mfa.recoveryDescription") : t("login.mfa.description") })
    ] }),
    /* @__PURE__ */ jsxs3("div", { className: "flex w-full flex-col gap-y-4", children: [
      isRecoveryCode ? /* @__PURE__ */ jsx3(
        Input2,
        {
          autoComplete: "one-time-code",
          className: "bg-ui-bg-field-component",
          placeholder: t("login.mfa.recoveryCodePlaceholder"),
          value: code,
          onChange: (e) => setCode(e.target.value),
          disabled: isLoading || !!verifiedToken
        }
      ) : /* @__PURE__ */ jsx3(
        OtpInput,
        {
          value: code,
          onChange: handleOtpChange,
          onComplete: handleVerify,
          disabled: isLoading || !!verifiedToken,
          autoFocus: true
        }
      ),
      error && /* @__PURE__ */ jsx3("div", { className: "text-center", children: /* @__PURE__ */ jsx3(Hint2, { className: "inline-flex", variant: "error", children: error }) }),
      /* @__PURE__ */ jsx3(
        Button3,
        {
          className: "w-full",
          isLoading,
          disabled: !verifiedToken && !canVerify,
          onClick: () => handleVerify(),
          children: t("login.mfa.verify")
        }
      ),
      onBack && /* @__PURE__ */ jsx3(
        Link,
        {
          to: "/login",
          className: "txt-small text-ui-fg-muted transition-fg hover:text-ui-fg-subtle focus-visible:text-ui-fg-subtle text-center outline-none",
          onClick: onBack,
          children: t("login.mfa.backToLogin")
        }
      )
    ] }),
    canUseRecoveryCode && /* @__PURE__ */ jsxs3("div", { className: "text-ui-fg-muted txt-small mt-6 text-center", children: [
      isRecoveryCode ? t("login.mfa.useAuthenticatorPrompt") : t("login.mfa.useRecoveryCodePrompt"),
      " ",
      /* @__PURE__ */ jsx3(
        "button",
        {
          type: "button",
          className: "text-ui-fg-interactive transition-fg hover:text-ui-fg-interactive-hover focus-visible:text-ui-fg-interactive-hover font-medium outline-none",
          onClick: () => handleMethodChange(isRecoveryCode ? "totp" : "recovery_code"),
          children: isRecoveryCode ? t("login.mfa.useAuthenticator") : t("login.mfa.useRecoveryCode")
        }
      )
    ] })
  ] });
};

// src/routes/login/components/mfa-challenge-card.tsx
import { jsx as jsx4, jsxs as jsxs4 } from "react/jsx-runtime";
var MfaChallengeCard = ({
  challenge,
  onSuccess,
  onBack
}) => {
  return /* @__PURE__ */ jsxs4("div", { className: "m-4 flex w-full max-w-[280px] flex-col items-center", children: [
    /* @__PURE__ */ jsx4(AvatarBox, {}),
    /* @__PURE__ */ jsx4(
      MfaChallengeForm,
      {
        challenge,
        onSuccess,
        onBack
      }
    )
  ] });
};

// src/routes/login/components/sso-login.tsx
import { Spinner as Spinner2 } from "@medusajs/icons";
import { Button as Button4, Text as Text2, toast as toast2 } from "@medusajs/ui";
import { useCallback as useCallback2, useEffect as useEffect2, useMemo, useRef as useRef2, useState as useState3 } from "react";
import { useTranslation as useTranslation4 } from "react-i18next";
import { decodeToken as decodeToken2 } from "react-jwt";
import { useNavigate as useNavigate3, useSearchParams as useSearchParams2 } from "react-router-dom";

// src/routes/login/utils.ts
var EMAILPASS_PROVIDER_ID = "emailpass";
var getRedirectProviders = (providers) => providers.filter((provider) => provider.flow === "redirect");
var hasEmailPassProvider = (providers) => providers.some((provider) => provider.id === EMAILPASS_PROVIDER_ID);

// src/routes/login/components/sso-login.tsx
import { jsx as jsx5, jsxs as jsxs5 } from "react/jsx-runtime";
var SsoLogin = ({ providers, onMfaChallenge }) => {
  const { t } = useTranslation4();
  const [searchParams] = useSearchParams2();
  const redirectProviders = useMemo(
    () => getRedirectProviders(providers),
    [providers]
  );
  const callbackProviderId = searchParams.get("auth_provider");
  const isCallback = !!callbackProviderId && (searchParams.has("code") || searchParams.has("error"));
  const showDivider = hasEmailPassProvider(providers);
  const { handleLogin, pendingProviderId } = useHandleLogin2();
  const { handleCallback, isCallbackPending } = useAuthCallback2(
    callbackProviderId,
    searchParams,
    onMfaChallenge
  );
  const actionInitiated = useRef2(false);
  useEffect2(() => {
    if (actionInitiated.current) {
      return;
    }
    if (isCallback) {
      actionInitiated.current = true;
      handleCallback();
    }
  }, [isCallback, handleCallback]);
  if (isCallback) {
    return /* @__PURE__ */ jsx5("div", { className: "bg-ui-bg-subtle fixed inset-0 z-50 flex items-center justify-center", children: /* @__PURE__ */ jsx5(Spinner2, { className: "text-ui-fg-subtle animate-spin" }) });
  }
  if (!redirectProviders.length) {
    return null;
  }
  const isPending = !!pendingProviderId || isCallbackPending;
  return /* @__PURE__ */ jsxs5("div", { className: "flex w-full flex-col gap-y-3", children: [
    showDivider && /* @__PURE__ */ jsx5(OrDivider, {}),
    redirectProviders.map((provider) => {
      return /* @__PURE__ */ jsx5(
        Button4,
        {
          variant: "secondary",
          onClick: () => handleLogin(provider.id),
          className: "w-full",
          disabled: isPending,
          isLoading: pendingProviderId === provider.id || isCallbackPending,
          children: t("auth.login.continueWithProvider", {
            provider: provider.display_name
          })
        },
        provider.id
      );
    })
  ] });
};
var OrDivider = () => {
  const { t } = useTranslation4();
  return /* @__PURE__ */ jsxs5("div", { className: "flex items-center gap-x-3", children: [
    /* @__PURE__ */ jsx5("hr", { className: "bg-ui-border-base h-px flex-1 border-0" }),
    /* @__PURE__ */ jsx5(Text2, { size: "small", leading: "compact", className: "text-ui-fg-muted", children: t("auth.login.or") }),
    /* @__PURE__ */ jsx5("hr", { className: "bg-ui-border-base h-px flex-1 border-0" })
  ] });
};
var useHandleLogin2 = () => {
  const { t } = useTranslation4();
  const [pendingProviderId, setPendingProviderId] = useState3(
    null
  );
  const handleLogin = useCallback2(
    async (providerId) => {
      setPendingProviderId(providerId);
      try {
        const result = await sdk.auth.login("user", providerId, {
          // setting callback_url in case the admin is on a different domain, or
          // the backend URL is set to just "/" which won't work for the callback
          callback_url: `${window.location.origin}${window.location.pathname}?auth_provider=${providerId}`
        });
        if (typeof result === "object" && "location" in result) {
          window.location.href = result.location;
          return;
        }
        throw new Error("Unexpected login response");
      } catch {
        toast2.error(t("auth.login.authenticationFailed"));
        setPendingProviderId(null);
      }
    },
    [t]
  );
  return { handleLogin, pendingProviderId };
};
var useAuthCallback2 = (providerId, searchParams, onMfaChallenge) => {
  const { t } = useTranslation4();
  const navigate = useNavigate3();
  const [isPending, setIsPending] = useState3(false);
  const ensureUser = useCallback2(
    async (token) => {
      const decodedToken = decodeToken2(token);
      if (!decodedToken?.actor_id && providerId) {
        await provisionAuthUser(providerId, token);
        const refreshedToken = await sdk.auth.refresh({
          Authorization: `Bearer ${token}`
          // passing it manually in case the auth type is session
        });
        if (!refreshedToken) {
          throw new Error("Failed to refresh token after user provisioning");
        }
      }
    },
    [providerId]
  );
  const handleCallback = useCallback2(async () => {
    if (!providerId) {
      return;
    }
    setIsPending(true);
    try {
      let token;
      try {
        const query = Object.fromEntries(searchParams);
        delete query.auth_provider;
        const result = await sdk.auth.callback("user", providerId, query);
        if (typeof result === "object" && "mfa_challenge" in result) {
          if (!onMfaChallenge) {
            throw new Error("MFA challenge handler is missing");
          }
          onMfaChallenge(result.mfa_challenge, async (verifiedToken) => {
            try {
              await ensureUser(verifiedToken);
              navigate("/");
            } catch (error) {
              toast2.error(resolveProvisionError(error, t));
              navigate("/login");
            }
          });
          return;
        }
        if (typeof result === "object" && "verification_required" in result) {
          throw new Error("Verification required but not implemented yet");
        }
        token = result;
      } catch (error) {
        throw new AuthCallbackError(t("auth.login.authenticationFailed"));
      }
      try {
        await ensureUser(token);
      } catch (error) {
        throw new AuthCallbackError(resolveProvisionError(error, t));
      }
      navigate("/");
    } catch (error) {
      toast2.error(
        error instanceof AuthCallbackError ? error.message : t("auth.login.authenticationFailed")
      );
      navigate("/login");
    }
    setIsPending(false);
  }, [providerId, searchParams, t, onMfaChallenge, ensureUser, navigate]);
  return { handleCallback, isCallbackPending: isPending };
};
var AuthCallbackError = class extends Error {
};
var resolveProvisionError = (error, t) => {
  if (isFetchError(error) && error.message) {
    return error.message;
  }
  return t("auth.login.provisioningFailed");
};

// src/routes/login/login.tsx
import { jsx as jsx6, jsxs as jsxs6 } from "react/jsx-runtime";
var Login = () => {
  const { t } = useTranslation5();
  const location = useLocation2();
  const navigate = useNavigate4();
  const { getWidgets } = useExtension();
  const [mfaChallenge, setMfaChallenge] = useState4(null);
  const [mfaSuccessHandler, setMfaSuccessHandler] = useState4(null);
  const from = location.state?.from?.pathname || "/orders";
  const { providers, isLoading: isProvidersLoading } = useAuthProviders();
  const providerList = useMemo2(() => providers ?? [], [providers]);
  const isEmailPassInstalled = useMemo2(
    () => hasEmailPassProvider(providerList),
    [providerList]
  );
  const handleMfaChallenge = (challenge, onSuccess) => {
    setMfaChallenge(challenge);
    setMfaSuccessHandler(() => onSuccess);
  };
  if (mfaChallenge) {
    return /* @__PURE__ */ jsx6("div", { className: "bg-ui-bg-subtle flex min-h-dvh w-dvw items-center justify-center", children: /* @__PURE__ */ jsx6(
      MfaChallengeCard,
      {
        challenge: mfaChallenge,
        onSuccess: (token) => {
          if (mfaSuccessHandler) {
            return mfaSuccessHandler(token);
          }
          navigate(from, { replace: true });
        },
        onBack: () => {
          setMfaChallenge(null);
          setMfaSuccessHandler(null);
        }
      }
    ) });
  }
  return /* @__PURE__ */ jsx6("div", { className: "bg-ui-bg-subtle flex min-h-dvh w-dvw items-center justify-center", children: /* @__PURE__ */ jsxs6("div", { className: "m-4 flex w-full max-w-[280px] flex-col items-center", children: [
    /* @__PURE__ */ jsx6(AvatarBox, {}),
    /* @__PURE__ */ jsxs6("div", { className: "mb-4 flex flex-col items-center", children: [
      /* @__PURE__ */ jsx6(Heading2, { children: t("login.title") }),
      /* @__PURE__ */ jsx6(Text3, { size: "small", className: "text-ui-fg-subtle text-center", children: t("login.hint") })
    ] }),
    /* @__PURE__ */ jsxs6("div", { className: "flex w-full flex-col gap-y-3", children: [
      getWidgets("login.before").map((Component, i) => {
        return /* @__PURE__ */ jsx6(Component, {}, i);
      }),
      isProvidersLoading ? /* @__PURE__ */ jsx6("div", { className: "flex w-full items-center justify-center py-6", children: /* @__PURE__ */ jsx6(Spinner3, { className: "text-ui-fg-subtle animate-spin" }) }) : !providerList.length ? /* @__PURE__ */ jsx6("div", { children: /* @__PURE__ */ jsx6(InlineTip, { label: t("general.tip"), children: t("auth.login.noProviders") }) }) : isEmailPassInstalled && /* @__PURE__ */ jsx6(EmailPassLogin, { onMfaChallenge: handleMfaChallenge }),
      getWidgets("login.after").map((Component, i) => {
        return /* @__PURE__ */ jsx6(Component, {}, i);
      }),
      /* @__PURE__ */ jsx6(CloudAuthLogin, { onMfaChallenge: handleMfaChallenge }),
      /* @__PURE__ */ jsx6(
        SsoLogin,
        {
          providers: providerList,
          onMfaChallenge: handleMfaChallenge
        }
      )
    ] }),
    isEmailPassInstalled && /* @__PURE__ */ jsx6("span", { className: "text-ui-fg-muted txt-small my-6", children: /* @__PURE__ */ jsx6(
      Trans,
      {
        i18nKey: "login.forgotPassword",
        components: [
          /* @__PURE__ */ jsx6(
            Link2,
            {
              to: "/reset-password",
              className: "text-ui-fg-interactive transition-fg hover:text-ui-fg-interactive-hover focus-visible:text-ui-fg-interactive-hover font-medium outline-none"
            },
            "reset-password-link"
          )
        ]
      }
    ) })
  ] }) });
};
export {
  Login as Component
};
